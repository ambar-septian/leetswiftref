---
id: 2487
title: "Remove Nodes From Linked List"
category: "linked-list"
difficulty: "Medium"
tags: ["Linked List", "Stack", "Recursion", "Monotonic Stack"]
leetcodeUrl: "https://leetcode.com/problems/remove-nodes-from-linked-list/description/"
notes: "Monotonic stack pattern to solve remove nodes from linked list by keeping only nodes not dominated by a greater value to their right."
---

```swift
/// Definition for singly-linked list.
/// public class ListNode {
///     public var val: Int
///     public var next: ListNode?
///     public init() { self.val = 0; self.next = nil; }
///     public init(_ val: Int) { self.val = val; self.next = nil; }
///     public init(_ val: Int, _ next: ListNode?) { self.val = val; self.next = next; }
/// }
class Solution {
  func removeNodes(_ head: ListNode?) -> ListNode? {
    var stack = [ListNode?]()

    var curr = head
    while curr != nil {
      while let top = stack.last??.val, curr!.val > top {
        stack.removeLast()
      }

      stack.append(curr)
      curr = curr?.next
    }

    var list = ListNode(0)
    curr = list
    for node in stack {
      curr?.next = node
      curr = curr?.next
    }

    return list.next
  }
}
```