---
id: 230
title: "Kth Smallest Element in a BST"
category: "binary-search-tree"
difficulty: "Medium"
tags: ["Tree", "Depth-First Search", "Binary Search Tree", "Binary Tree"]
leetcodeUrl: "https://leetcode.com/problems/kth-smallest-element-in-a-bst/description/"
notes: "Iterative inorder traversal pattern to solve kth smallest in BST by using a stack to traverse left branches and counting visited nodes until the kth is reached."
---

```swift
/// Definition for a binary tree node.
/// public class TreeNode {
///     public var val: Int
///     public var left: TreeNode?
///     public var right: TreeNode?
///     public init() { self.val = 0; self.left = nil; self.right = nil; }
///     public init(_ val: Int) { self.val = val; self.left = nil; self.right = nil; }
///     public init(_ val: Int, _ left: TreeNode?, _ right: TreeNode?) {
///         self.val = val
///         self.left = left
///         self.right = right
///     }
/// }
class Solution {
  func kthSmallest(_ root: TreeNode?, _ k: Int) -> Int {
    var stack: [TreeNode?] = []
    var curr: TreeNode? = root
    var n = 0

    while !stack.isEmpty || curr != nil {
      while curr != nil {
        stack.append(curr)
        curr = curr?.left
      }

      curr = stack.removeLast()
      n += 1

      if n == k {
        return curr!.val
      } else {
        curr = curr?.right
      }
    }

    return n
  }
}
```