---
id: 2807
title: "Insert Greatest Common Divisors in Linked List"
category: "linked-list"
difficulty: "Medium"
tags: ["Linked List", "Math", "Number Theory"]
leetcodeUrl: "https://leetcode.com/problems/insert-greatest-common-divisors-in-linked-list/description/"
notes: "Euclidean GCD pattern to solve insert greatest common divisors in linked list by computing the GCD of each adjacent pair and inserting a new node between them."
---

```swift
/// Definition for singly-linked list.
/// public class ListNode {
///     public var val: Int
///     public var next: ListNode?
///     public init() { self.val = 0; self.next = nil; }
///     public init(_ val: Int) { self.val = val; self.next = nil; }
///     public init(_ val: Int, _ next: ListNode?) { self.val = val; self.next = next; }
/// }

class Solution {
  func insertGreatestCommonDivisors(_ head: ListNode?) -> ListNode? {
    var current = head

    while current?.next != nil {
      if let next = current?.next {
        let gcd = self.findGCD(current!.val, next.val)
        current?.next = gcd
        gcd.next = next
      }

      current = current?.next?.next
    }

    return head
  }

  func findGCD(_ a: Int, _ b: Int) -> ListNode {
    var x = a
    var y = b

    while y != 0 {
      let temp = y
      y = x % y
      x = temp
    }

    return ListNode(x)
  }
}
```