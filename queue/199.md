---
id: 199
title: "Binary Tree Right Side View"
category: "binary-tree"
difficulty: "Medium"
tags: ["Tree", "Depth-First Search", "Breadth-First Search", "Binary Tree"]
leetcodeUrl: "https://leetcode.com/problems/binary-tree-right-side-view/description/"
notes: "BFS level-order traversal pattern to solve binary tree right side view by recording only the last node's value processed at each level."
---

```swift
/// Definition for a binary tree node.
/// public class TreeNode {
///     public var val: Int
///     public var left: TreeNode?
///     public var right: TreeNode?
///     public init() { self.val = 0; self.left = nil; self.right = nil; }
///     public init(_ val: Int) { self.val = val; self.left = nil; self.right = nil; }
///     public init(_ val: Int, _ left: TreeNode?, _ right: TreeNode?) {
///         self.val = val
///         self.left = left
///         self.right = right
///     }
/// }
class Solution {
  func rightSideView(_ root: TreeNode?) -> [Int] {
    guard let root else { return [] }
    var queue: [TreeNode?] = [root]
    var result = [Int]()

    while !queue.isEmpty {
      let count = queue.count

      for i in 0..<count {
        guard let item = queue.removeFirst() else { continue }
        if i == count - 1 {
          result.append(item.val)
        }

        if let left = item.left {
          queue.append(left)
        }

        if let right = item.right {
          queue.append(right)
        }

      }
    }

    return result

  }
}
```