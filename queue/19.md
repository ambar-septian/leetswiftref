---
id: 19
title: "Remove Nth Node From End of List"
category: "linked-list"
difficulty: "Medium"
tags: ["Linked List", "Two Pointers"]
leetcodeUrl: "https://leetcode.com/problems/remove-nth-node-from-end-of-list/description/"
notes: "Fast/slow pointer pattern to solve remove Nth node from end by using two-pointer distance counting to find the target position before deletion."
---

```swift
/// Definition for singly-linked list.
/// public class ListNode {
///     public var val: Int
///     public var next: ListNode?
///     public init() { self.val = 0; self.next = nil; }
///     public init(_ val: Int) { self.val = val; self.next = nil; }
///     public init(_ val: Int, _ next: ListNode?) { self.val = val; self.next = next; }
/// }
class Solution {
  func removeNthFromEnd(_ head: ListNode?, _ n: Int) -> ListNode? {
    var slow = head
    var fast = head?.next
    var count = 1

    while fast != nil, fast?.next != nil {
      slow = slow?.next
      fast = fast?.next?.next
      count += 1
    }

    count = count * 2
    if fast == nil { count -= 1 }
    let target = max(0, count - n)
    var dummy: ListNode? = ListNode(0)
    var curr = dummy
    curr?.next = head

    for i in 0..<target {
      curr = curr?.next

    }
    curr?.next = curr?.next?.next
    return dummy?.next
  }
}
```