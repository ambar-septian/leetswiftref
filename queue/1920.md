---
id: 1920
title: "Build Array from Permutation"
category: "array"
difficulty: "Easy"
tags: ["Array", "Simulation"]
leetcodeUrl: "https://leetcode.com/problems/build-array-from-permutation/description/"
notes: "Simulation pattern to solve build array from permutation by constructing each result element as nums[nums[i]] in a single pass."
---

```swift
class Solution {
  func buildArray(_ nums: [Int]) -> [Int] {
    var results = [Int]()
    for i in 0..<nums.count {
      results.append(nums[nums[i]])
    }

    return results
  }
}
```