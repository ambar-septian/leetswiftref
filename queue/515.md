---
id: 515
title: "Find Largest Value in Each Tree Row"
category: "binary-tree"
difficulty: "Medium"
tags: ["Tree", "Depth-First Search", "Breadth-First Search", "Binary Tree"]
leetcodeUrl: "https://leetcode.com/problems/find-largest-value-in-each-tree-row/description/"
notes: "BFS level-order traversal pattern to solve find largest value in each tree row by tracking the maximum value seen while processing each level's nodes."
---

```swift
/// Definition for a binary tree node.
/// public class TreeNode {
///     public var val: Int
///     public var left: TreeNode?
///     public var right: TreeNode?
///     public init() { self.val = 0; self.left = nil; self.right = nil; }
///     public init(_ val: Int) { self.val = val; self.left = nil; self.right = nil; }
///     public init(_ val: Int, _ left: TreeNode?, _ right: TreeNode?) {
///         self.val = val
///         self.left = left
///         self.right = right
///     }
/// }
class Solution {
  func largestValues(_ root: TreeNode?) -> [Int] {
    guard let root else { return [] }

    var queue = [root]
    var results = [Int]()

    while !queue.isEmpty {
      let count = queue.count
      var maxValue = Int.min

      for i in 0..<count {
        let item = queue.removeFirst()
        maxValue = max(maxValue, item.val)

        if let left = item.left {
          queue.append(left)
        }

        if let right = item.right {
          queue.append(right)
        }
      }

      results.append(maxValue)
    }

    return results
  }
}
```