---
id: 2130
title: "Maximum Twin Sum of a Linked List"
category: "linked-list"
difficulty: "Medium"
tags: ["Linked List", "Two Pointers", "Stack"]
leetcodeUrl: "https://leetcode.com/problems/maximum-twin-sum-of-a-linked-list/description/"
notes: "Fast/slow pointer and stack pattern to solve maximum twin sum of a linked list by splitting the list at the midpoint and pairing front values from the stack with back values."
---

```swift
/// Definition for singly-linked list.
/// public class ListNode {
///     public var val: Int
///     public var next: ListNode?
///     public init() { self.val = 0; self.next = nil; }
///     public init(_ val: Int) { self.val = val; self.next = nil; }
///     public init(_ val: Int, _ next: ListNode?) { self.val = val; self.next = next; }
/// }
class Solution {
  func pairSum(_ head: ListNode?) -> Int {
    var slow = head
    var fast = head
    var capacity = 0

    while fast?.next != nil, fast?.next?.next != nil {
      slow = slow?.next
      fast = fast?.next?.next
      capacity += 2
    }

    capacity = min(2, capacity * 2)

    var curr = head
    var stack = [Int]()
    stack.reserveCapacity(capacity)

    var result = 0
    var passMiddle = false

    while curr != nil {
      if !passMiddle, curr === slow?.next {
        passMiddle = true
      }
      if passMiddle {
        let last = stack.removeLast()
        result = max(last + curr!.val, result)
      } else {
        stack.append(curr!.val)
      }

      curr = curr?.next
    }

    return result
  }
}
```