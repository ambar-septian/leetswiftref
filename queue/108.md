---
id: 108
title: "Convert Sorted Array to Binary Search Tree"
category: "binary-search-tree"
difficulty: "Easy"
tags: ["Array", "Divide and Conquer", "Tree", "Binary Search Tree", "Binary Tree"]
leetcodeUrl: "https://leetcode.com/problems/convert-sorted-array-to-binary-search-tree/description/"
notes: "Divide and conquer pattern to solve sorted array to BST by always choosing the midpoint as the root and recursively building balanced left and right subtrees."
---

```swift
/// Definition for a binary tree node.
/// public class TreeNode {
///     public var val: Int
///     public var left: TreeNode?
///     public var right: TreeNode?
///     public init() { self.val = 0; self.left = nil; self.right = nil; }
///     public init(_ val: Int) { self.val = val; self.left = nil; self.right = nil; }
///     public init(_ val: Int, _ left: TreeNode?, _ right: TreeNode?) {
///         self.val = val
///         self.left = left
///         self.right = right
///     }
/// }
class Solution {
  func sortedArrayToBST(_ nums: [Int]) -> TreeNode? {
    func recursive(l: Int, r: Int) -> TreeNode? {
      guard l <= r else { return nil }
      let m = (l + r) / 2
      let tree = TreeNode(nums[m])
      tree.left = recursive(l: l, r: m - 1)
      tree.right = recursive(l: m + 1, r: r)

      return tree
    }

    return recursive(l: 0, r: nums.count - 1)
  }
}
```