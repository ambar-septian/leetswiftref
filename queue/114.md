---
id: 114
title: "Flatten Binary Tree to Linked List"
category: "linked-list"
difficulty: "Medium"
tags: ["Linked List", "Stack", "Tree", "Depth-First Search", "Binary Tree"]
leetcodeUrl: "https://leetcode.com/problems/flatten-binary-tree-to-linked-list/description/"
notes: "Post-order DFS pattern to solve flatten binary tree to linked list by recursively returning each subtree's tail so the left tail can be stitched to the right subtree."
---

```swift
/// Definition for a binary tree node.
/// public class TreeNode {
///     public var val: Int
///     public var left: TreeNode?
///     public var right: TreeNode?
///     public init() { self.val = 0; self.left = nil; self.right = nil; }
///     public init(_ val: Int) { self.val = val; self.left = nil; self.right = nil; }
///     public init(_ val: Int, _ left: TreeNode?, _ right: TreeNode?) {
///         self.val = val
///         self.left = left
///         self.right = right
///     }
/// }
class Solution {
  func flatten(_ root: TreeNode?) {

    func dfs(_ root: TreeNode?) -> TreeNode? {
      guard let root else {
        return nil
      }

      let leftTail = dfs(root.left)
      let rightTail = dfs(root.right)

      // cek leftTail biar bisa dijahit ke kanan
      if let leftTail {
        leftTail.right = root.right
        root.right = root.left
        root.left = nil
      }

      return rightTail ?? leftTail ?? root
    }

    dfs(root)
  }
}
```