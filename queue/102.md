---
id: 102
title: "Binary Tree Level Order Traversal"
category: "binary-tree"
difficulty: "Medium"
tags: ["Tree", "Breadth-First Search", "Binary Tree"]
leetcodeUrl: "https://leetcode.com/problems/binary-tree-level-order-traversal/description/"
notes: "BFS level-order traversal pattern to solve binary tree level order by processing all nodes of the current queue snapshot before moving to the next level."
---

```swift
/// Definition for a binary tree node.
/// public class TreeNode {
///     public var val: Int
///     public var left: TreeNode?
///     public var right: TreeNode?
///     public init() { self.val = 0; self.left = nil; self.right = nil; }
///     public init(_ val: Int) { self.val = val; self.left = nil; self.right = nil; }
///     public init(_ val: Int, _ left: TreeNode?, _ right: TreeNode?) {
///         self.val = val
///         self.left = left
///         self.right = right
///     }
/// }
class Solution {
  func levelOrder(_ root: TreeNode?) -> [[Int]] {
    guard let root else { return [] }

    var queue: [TreeNode?] = [root]
    var result = [[Int]]()

    while !queue.isEmpty {
      let count = queue.count
      var curr = [Int]()
      for i in 0..<count {
        guard let item = queue.removeFirst() else {
          continue
        }

        curr.append(item.val)
        queue.append(item.left)
        queue.append(item.right)
      }

      guard !curr.isEmpty else { continue }
      result.append(curr)
    }

    return result
  }
}
```