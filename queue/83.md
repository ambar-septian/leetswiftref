---
id: 83
title: "Remove Duplicates from Sorted List"
category: "linked-list"
difficulty: "Easy"
tags: ["Linked List"]
leetcodeUrl: "https://leetcode.com/problems/remove-duplicates-from-sorted-list/description/"
notes: "Iterative traversal pattern to solve remove duplicates from sorted list by skipping next nodes whose value equals the current node's value."
---

```swift
/// Definition for singly-linked list.
/// public class ListNode {
///     public var val: Int
///     public var next: ListNode?
///     public init() { self.val = 0; self.next = nil; }
///     public init(_ val: Int) { self.val = val; self.next = nil; }
///     public init(_ val: Int, _ next: ListNode?) { self.val = val; self.next = next; }
/// }
class Solution {
  func deleteDuplicates(_ head: ListNode?) -> ListNode? {
    var curr = head
    var result = curr

    while curr != nil {
      var item = curr
      while item?.val == item?.next?.val {
        item?.next = item?.next?.next
      }

      curr = item?.next
    }

    return result
  }
}
```