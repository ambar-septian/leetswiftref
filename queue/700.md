---
id: 700
title: "Search in a Binary Search Tree"
category: "binary-search-tree"
difficulty: "Easy"
tags: ["Tree", "Binary Search Tree", "Binary Tree"]
leetcodeUrl: "https://leetcode.com/problems/search-in-a-binary-search-tree/description/"
notes: "Iterative BST traversal pattern to solve search in BST by moving left when the target is smaller and right when it is larger until a match or null is found."
---

```swift
/// Definition for a binary tree node.
/// public class TreeNode {
///     public var val: Int
///     public var left: TreeNode?
///     public var right: TreeNode?
///     public init() { self.val = 0; self.left = nil; self.right = nil; }
///     public init(_ val: Int) { self.val = val; self.left = nil; self.right = nil; }
///     public init(_ val: Int, _ left: TreeNode?, _ right: TreeNode?) {
///         self.val = val
///         self.left = left
///         self.right = right
///     }
/// }
class Solution {
  func searchBST(_ root: TreeNode?, _ val: Int) -> TreeNode? {

    var next = root

    while let curr = next {
      if curr.val == val {
        return curr
      }

      if curr.val > val {
        next = curr.left
      } else {
        next = curr.right
      }
    }

    return next
  }
}
```