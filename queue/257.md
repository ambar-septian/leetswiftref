---
id: 257
title: "Binary Tree Paths"
category: "backtracking"
difficulty: "Easy"
tags: ["String", "Backtracking", "Tree", "Depth-First Search", "Binary Tree"]
leetcodeUrl: "https://leetcode.com/problems/binary-tree-paths/description/"
notes: "Backtracking DFS pattern to solve binary tree paths by building a string path and recording it when a leaf is reached, then removing the last element on the way back."
---

```swift
/// Definition for a binary tree node.
/// public class TreeNode {
///     public var val: Int
///     public var left: TreeNode?
///     public var right: TreeNode?
///     public init() { self.val = 0; self.left = nil; self.right = nil; }
///     public init(_ val: Int) { self.val = val; self.left = nil; self.right = nil; }
///     public init(_ val: Int, _ left: TreeNode?, _ right: TreeNode?) {
///         self.val = val
///         self.left = left
///         self.right = right
///     }
/// }
class Solution {
  func binaryTreePaths(_ root: TreeNode?) -> [String] {
    guard let root else { return [] }
    var results = [String]()
    var path = [String]()

    func dfs(_ root: TreeNode?) {
      guard let root else {
        return
      }

      path.append(String(root.val))

      if root.left == nil, root.right == nil {
        let stringVal = path.joined(separator: "->")
        results.append(stringVal)
        path.removeLast()

        return
      }

      dfs(root.left)
      dfs(root.right)

      path.removeLast()
    }

    dfs(root)
    return results
  }
}
```