---
id: 104
title: "Maximum Depth of Binary Tree"
category: "binary-tree"
difficulty: "Easy"
tags: ["Tree", "Depth-First Search", "Breadth-First Search", "Binary Tree"]
leetcodeUrl: "https://leetcode.com/problems/maximum-depth-of-binary-tree/description/"
notes: "Recursive DFS pattern to solve maximum depth of binary tree by returning 1 plus the greater of the left and right subtree depths."
---

```swift
/// Definition for a binary tree node.
/// public class TreeNode {
///     public var val: Int
///     public var left: TreeNode?
///     public var right: TreeNode?
///     public init() { self.val = 0; self.left = nil; self.right = nil; }
///     public init(_ val: Int) { self.val = val; self.left = nil; self.right = nil; }
///     public init(_ val: Int, _ left: TreeNode?, _ right: TreeNode?) {
///         self.val = val
///         self.left = left
///         self.right = right
///     }
/// }
class Solution {
  func maxDepth(_ root: TreeNode?) -> Int {
    guard let root else { return 0 }
    return dfs(root)

    func dfs(_ node: TreeNode?) -> Int {
      guard let node else { return 0 }

      let left = dfs(node.left)
      let right = dfs(node.right)

      return 1 + max(left, right)
    }
  }
}
```