---
id: 1071
title: "Greatest Common Divisor of Strings"
category: "math"
difficulty: "Easy"
tags: ["Math", "String"]
leetcodeUrl: "https://leetcode.com/problems/greatest-common-divisor-of-strings/description/"
notes: "GCD math pattern to solve greatest common divisor of strings by verifying the strings are commutative under concatenation and then taking the GCD-length prefix."
---

```swift
class Solution {
  func gcdOfStrings(_ str1: String, _ str2: String) -> String {
    guard str1 + str2 == str2 + str1 else { return "" }
    let gcdResult = gcd(str1.count, str2.count)

    let result = str1.prefix(gcdResult)

    return String(result)
  }

  func gcd(_ x: Int, _ y: Int) -> Int {
    var curr = y
    var result = x

    while curr != 0 {
      let temp = curr
      curr = result % temp
      result = temp
    }

    return result
  }
}
```