---
id: 116
title: "Populating Next Right Pointers in Each Node"
category: "linked-list"
difficulty: "Medium"
tags: ["Linked List", "Tree", "Depth-First Search", "Breadth-First Search", "Binary Tree"]
leetcodeUrl: "https://leetcode.com/problems/populating-next-right-pointers-in-each-node/description/"
notes: "Level-by-level traversal pattern to solve populating next right pointers by walking each level with the existing next pointers to link children without extra space."
---

```swift
/**
 * Definition for a Node.
 * public class Node {
 *     public var val: Int
 *     public var left: Node?
 *     public var right: Node?
 *	   public var next: Node?
 *     public init(_ val: Int) {
 *         self.val = val
 *         self.left = nil
 *         self.right = nil
 *         self.next = nil
 *     }
 * }
 */

class Solution {
  func connect(_ root: Node?) -> Node? {
    guard let root else { return nil }

    var leftMost: Node? = root

    while leftMost?.left != nil {
      var curr = leftMost

      while curr != nil {
        curr?.left?.next = curr?.right

        if let next = curr?.next {
          curr?.right?.next = next.left
        }

        curr = curr?.next
      }

      leftMost = leftMost?.left
    }

    return root

  }

  func connectBFS(_ root: Node?) -> Node? {
    guard let root else { return nil }

    var queue = [root]

    while !queue.isEmpty {
      let count = queue.count

      for i in 0..<count {
        let node = queue.removeFirst()

        if i != count - 1 {
          node.next = queue.first
        }

        if let left = node.left {
          queue.append(left)
        }

        if let right = node.right {
          queue.append(right)
        }
      }
    }

    return root
  }
}
```