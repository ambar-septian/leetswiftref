---
id: 450
title: "Delete Node in a BST"
category: "binary-search-tree"
difficulty: "Medium"
tags: ["Tree", "Binary Search Tree", "Binary Tree"]
leetcodeUrl: "https://leetcode.com/problems/delete-node-in-a-bst/description/"
notes: "Recursive BST pattern to solve delete node in BST by navigating to the target, replacing it with the in-order successor when it has two children."
---

```swift
/// Definition for a binary tree node.
/// public class TreeNode {
///     public var val: Int
///     public var left: TreeNode?
///     public var right: TreeNode?
///     public init() { self.val = 0; self.left = nil; self.right = nil; }
///     public init(_ val: Int) { self.val = val; self.left = nil; self.right = nil; }
///     public init(_ val: Int, _ left: TreeNode?, _ right: TreeNode?) {
///         self.val = val
///         self.left = left
///         self.right = right
///     }
/// }
class Solution {
  func deleteNode(_ root: TreeNode?, _ key: Int) -> TreeNode? {
    guard let root else {
      return root
    }

    if key > root.val {
      root.right = deleteNode(root.right, key)
    } else if key < root.val {
      root.left = deleteNode(root.left, key)
    } else {
      if root.left == nil {
        return root.right
      } else if root.right == nil {
        return root.left
      }

      // Find the min right
      var curr = root.right
      while let left = curr?.left {
        curr = left
      }

      root.val = curr?.val ?? -1
      root.right = deleteNode(root.right, root.val)

      return root
    }

    return root
  }
}
```