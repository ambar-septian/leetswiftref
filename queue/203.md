---
id: 203
title: "Remove Linked List Elements"
category: "linked-list"
difficulty: "Easy"
tags: ["Linked List", "Recursion"]
leetcodeUrl: "https://leetcode.com/problems/remove-linked-list-elements/description/"
notes: "Dummy head pointer pattern to solve remove linked list elements by checking the next node's value and bypassing it when it matches the target."
---

```swift
/// Definition for singly-linked list.
/// public class ListNode {
///     public var val: Int
///     public var next: ListNode?
///     public init() { self.val = 0; self.next = nil; }
///     public init(_ val: Int) { self.val = val; self.next = nil; }
///     public init(_ val: Int, _ next: ListNode?) { self.val = val; self.next = next; }
/// }
class Solution {
  func removeElements(_ head: ListNode?, _ val: Int) -> ListNode? {
    var curr: ListNode? = ListNode()
    var result = curr
    curr?.next = head

    while curr != nil {
      if curr?.next?.val == val {
        curr?.next = curr?.next?.next
      } else {
        curr = curr?.next
      }
    }

    return result?.next
  }
}
```