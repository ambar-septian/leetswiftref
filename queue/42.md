---
id: 42
title: "Trapping Rain Water"
category: "dp-1d"
difficulty: "Hard"
tags: ["Array", "Two Pointers", "Dynamic Programming", "Stack", "Monotonic Stack"]
leetcodeUrl: "https://leetcode.com/problems/trapping-rain-water/description/"
notes: "Two-pointer pattern to solve trapping rain water by tracking left and right max heights and accumulating trapped water on whichever side has the lower maximum."
---

```swift
class Solution {
  func trap(_ height: [Int]) -> Int {
    guard !height.isEmpty else { return 0 }

    var l = 0
    var r = height.count - 1
    var leftMax = height[l]
    var rightMax = height[r]
    var result = 0

    while l < r {
      if leftMax < rightMax {
        l += 1
        leftMax = max(leftMax, height[l])
        result += leftMax - height[l]
      } else {
        r -= 1
        rightMax = max(rightMax, height[r])
        result += rightMax - height[r]
      }
    }

    return result
  }

  func trapMonotonic(_ height: [Int]) -> Int {
    var stack = [(index: Int, value: Int)]()

    return 0
  }
}
```