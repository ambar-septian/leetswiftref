---
id: 100
title: "Same Tree"
category: "binary-tree"
difficulty: "Easy"
tags: ["Tree", "Depth-First Search", "Breadth-First Search", "Binary Tree"]
leetcodeUrl: "https://leetcode.com/problems/same-tree/description/"
notes: "DFS serialization pattern to solve same tree by converting both trees to preorder arrays including null sentinels and comparing the resulting sequences."
---

```swift
/// Definition for a binary tree node.
/// public class TreeNode {
///     public var val: Int
///     public var left: TreeNode?
///     public var right: TreeNode?
///     public init() { self.val = 0; self.left = nil; self.right = nil; }
///     public init(_ val: Int) { self.val = val; self.left = nil; self.right = nil; }
///     public init(_ val: Int, _ left: TreeNode?, _ right: TreeNode?) {
///         self.val = val
///         self.left = left
///         self.right = right
///     }
/// }
class Solution {
  func isSameTree(_ p: TreeNode?, _ q: TreeNode?) -> Bool {
    func dfs(_ tree: TreeNode?) -> [Int?] {
      guard let tree else { return [nil] }

      return [tree.val] + dfs(tree.left) + dfs(tree.right)

    }

    return dfs(p) == dfs(q)
  }
}
```