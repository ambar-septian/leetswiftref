---
id: 374
title: "Guess Number Higher or Lower"
category: "binary-search"
difficulty: "Easy"
tags: ["Binary Search", "Interactive"]
leetcodeUrl: "https://leetcode.com/problems/guess-number-higher-or-lower/description/"
notes: "Binary search pattern to solve guess number higher or lower by halving the search space based on the API's feedback until the correct number is found."
---

```swift
/**
 * Forward declaration of guess API.
 * @param  num -> your guess number
 * @return 	     -1 if num is higher than the picked number
 *			      1 if num is lower than the picked number
 *               otherwise return 0
 * func guess(_ num: Int) -> Int
 */

class Solution: GuessGame {
  func guessNumber(_ n: Int) -> Int {

    var l = 0
    var r = n

    while true {
      let middle = (l + r) / 2
      let result = guess(middle)
      if result == 0 {
        return middle
      } else if result == 1 {
        l = middle + 1
      } else if result == -1 {
        r = middle - 1
      }
    }

    return -1
  }
}
```