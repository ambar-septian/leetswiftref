---
id: 1161
title: "Maximum Level Sum of a Binary Tree"
category: "binary-tree"
difficulty: "Medium"
tags: ["Tree", "Depth-First Search", "Breadth-First Search", "Binary Tree"]
leetcodeUrl: "https://leetcode.com/problems/maximum-level-sum-of-a-binary-tree/description/"
notes: "BFS level-order traversal pattern to solve maximum level sum by summing node values per level and tracking the level with the highest sum."
---

```swift
/// Definition for a binary tree node.
/// public class TreeNode {
///     public var val: Int
///     public var left: TreeNode?
///     public var right: TreeNode?
///     public init() { self.val = 0; self.left = nil; self.right = nil; }
///     public init(_ val: Int) { self.val = val; self.left = nil; self.right = nil; }
///     public init(_ val: Int, _ left: TreeNode?, _ right: TreeNode?) {
///         self.val = val
///         self.left = left
///         self.right = right
///     }
/// }
class Solution {
  func maxLevelSum(_ root: TreeNode?) -> Int {
    guard let root else { return 0 }

    var queue = [root]
    var currentLevel = 0
    var result = 0
    var maxVal = Int.min

    while !queue.isEmpty {
      let count = queue.count

      var sum = 0
      for i in 0..<count {
        let node = queue.removeFirst()
        sum += node.val

        if let left = node.left {
          queue.append(left)
        }

        if let right = node.right {
          queue.append(right)
        }
      }

      currentLevel += 1

      if sum > maxVal {
        maxVal = sum
        result = currentLevel
      }
    }

    return result
  }
}
```