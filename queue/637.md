---
id: 637
title: "Average of Levels in Binary Tree"
category: "binary-tree"
difficulty: "Easy"
tags: ["Tree", "Depth-First Search", "Breadth-First Search", "Binary Tree"]
leetcodeUrl: "https://leetcode.com/problems/average-of-levels-in-binary-tree/description/"
notes: "BFS level-order traversal pattern to solve average of levels by summing all node values per level and dividing by the level's node count."
---

```swift
/// Definition for a binary tree node.
/// public class TreeNode {
///     public var val: Int
///     public var left: TreeNode?
///     public var right: TreeNode?
///     public init() { self.val = 0; self.left = nil; self.right = nil; }
///     public init(_ val: Int) { self.val = val; self.left = nil; self.right = nil; }
///     public init(_ val: Int, _ left: TreeNode?, _ right: TreeNode?) {
///         self.val = val
///         self.left = left
///         self.right = right
///     }
/// }
class Solution {
  func averageOfLevels(_ root: TreeNode?) -> [Double] {
    guard let root else { return [] }
    var queue = [root]
    var results = [Double]()

    while !queue.isEmpty {
      let count = queue.count

      var sum = 0
      for i in 0..<count {
        let item = queue.removeFirst()
        sum += item.val

        if let left = item.left {
          queue.append(left)
        }

        if let right = item.right {
          queue.append(right)
        }
      }

      if count != 0 {
        let result = Double(sum) / Double(count)
        results.append(result)
      }
    }

    return results
  }
}
```