---
id: 543
title: "Diameter of Binary Tree"
category: "binary-tree"
difficulty: "Easy"
tags: ["Tree", "Depth-First Search", "Binary Tree"]
leetcodeUrl: "https://leetcode.com/problems/diameter-of-binary-tree/description/"
notes: "Post-order DFS pattern to solve diameter of binary tree by updating a global max with the sum of left and right heights at each node."
---

```swift
/// Definition for a binary tree node.
/// public class TreeNode {
///     public var val: Int
///     public var left: TreeNode?
///     public var right: TreeNode?
///     public init() { self.val = 0; self.left = nil; self.right = nil; }
///     public init(_ val: Int) { self.val = val; self.left = nil; self.right = nil; }
///     public init(_ val: Int, _ left: TreeNode?, _ right: TreeNode?) {
///         self.val = val
///         self.left = left
///         self.right = right
///     }
/// }
class Solution {
  func diameterOfBinaryTree(_ root: TreeNode?) -> Int {
    var result = 0

    func recursive(_ root: TreeNode?) -> Int {
      guard let root else { return 0 }
      let left = recursive(root.left)
      let right = recursive(root.right)
      result = max(result, left + right)
      return 1 + max(left, right)
    }

    recursive(root)
    return result
  }
}
```