---
id: 129
title: "Sum Root to Leaf Numbers"
category: "binary-tree"
difficulty: "Medium"
tags: ["Tree", "Depth-First Search", "Binary Tree"]
leetcodeUrl: "https://leetcode.com/problems/sum-root-to-leaf-numbers/description/"
notes: "DFS pattern to solve sum root to leaf numbers by passing the accumulated number down each path and returning the full number at each leaf."
---

```swift
/// Definition for a binary tree node.
/// public class TreeNode {
///     public var val: Int
///     public var left: TreeNode?
///     public var right: TreeNode?
///     public init() { self.val = 0; self.left = nil; self.right = nil; }
///     public init(_ val: Int) { self.val = val; self.left = nil; self.right = nil; }
///     public init(_ val: Int, _ left: TreeNode?, _ right: TreeNode?) {
///         self.val = val
///         self.left = left
///         self.right = right
///     }
/// }
class Solution {
  func sumNumbers(_ root: TreeNode?) -> Int {
    func dfs(_ root: TreeNode?, _ prev: Int) -> Int {
      guard let root else { return 0 }

      let val = root.val
      let curr = prev * 10 + val

      if root.left == nil && root.right == nil {
        return curr
      }

      let left = dfs(root.left, curr)
      let right = dfs(root.right, curr)
      return left + right
    }

    return dfs(root, 0)
  }
}
```