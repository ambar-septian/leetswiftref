---
id: 210-course-schedule-ii
title: "Course Schedule II"
hide_table_of_contents: true
---

import { ProblemHeader, SectionHeader, ComplexityTable, PatternFlowchart, Walkthrough, StepList, StepItem, CommonPitfalls, Callout } from '@site/src/components/DSA';
import CourseScheduleIIDiagram from '@site/src/components/DSA/CourseScheduleIIDiagram';

<ProblemHeader
  number={210}
  title="Course Schedule II"
  difficulty="Medium"
  pattern="Topological Sort (Kahn's Algorithm)"
  tags={["Depth-First Search", "Breadth-First Search", "Graph Theory", "Topological Sort"]}
  leetcodeUrl="https://leetcode.com/problems/course-schedule-ii/description/"
/>

<SectionHeader n={1} title="Problem Overview" />

There are a total of `numCourses` courses you have to take, labelled from `0` to `numCourses - 1`. You are given an array `prerequisites` where `prerequisites[i] = [a, b]` indicates that you must take course `b` before course `a`.

Return the ordering of courses you should take to finish all courses. If there are multiple valid answers, return any of them. If it is impossible to finish all courses (i.e. a cycle exists), return an empty array.

**Constraints:**
- `1 <= numCourses <= 2000`
- `0 <= prerequisites.length <= numCourses * (numCourses - 1)`
- `prerequisites[i].length == 2`
- `0 <= a, b < numCourses`
- `a != b`
- All the pairs `[a, b]` are distinct.

<SectionHeader n={2} title="Examples" />

#### Example 1

```
Input: numCourses = 2, prerequisites = [[1,0]]
Output: [0,1]
```

#### Example 2

```
Input: numCourses = 4, prerequisites = [[1,0],[2,0],[3,1],[3,2]]
Output: [0,2,1,3]  (or any valid topological order)
```

#### Example 3

```
Input: numCourses = 2, prerequisites = [[1,0],[0,1]]
Output: []  (cycle detected)
```

<SectionHeader n={3} title="Pattern Recognition" />

<PatternFlowchart
  steps={[
    { question: "Do we need to return the order, not just a bool?", verdict: "yes", reason: "Kahn's BFS naturally produces the topological order as it processes nodes — just collect them into a result array." },
    { question: "How do we detect a cycle?", verdict: "yes", reason: "If results.count is less than numCourses after BFS, some nodes never reached in-degree 0 — a cycle exists." },
    { question: "Is the order unique?", verdict: "no", reason: "Multiple topological orderings may exist; any valid one is accepted." },
  ]}
  answer="Kahn's BFS — collect processed nodes in results; return results if count == numCourses, else []"
/>

<SectionHeader n={4} title="Approach" />

<CourseScheduleIIDiagram />

Build an adjacency list and in-degree array. Seed a queue with all zero-in-degree courses. Process each course: append it to `results`, decrement each dependent course's in-degree, and enqueue dependents that hit zero. If `results.count == numCourses`, a valid topological order exists; otherwise return `[]`.

<Callout type="cyan" label="Difference from Course Schedule I">
Problem 207 only needs a boolean — cycle or no cycle. Problem 210 needs the actual order. The BFS body is identical; the only change is collecting nodes into <code>results</code> and returning the array instead of a count comparison.
</Callout>

<SectionHeader n={5} title="Interactive Walkthrough" />

<Walkthrough
  input="numCourses = 4, prerequisites = [[1,0],[2,0],[3,1],[3,2]]"
  expected="[0,1,2,3]"
  steps={[
    { label: "build",  text: "adjList: {0:[1,2], 1:[3], 2:[3]}; inDegree: [0,1,1,2]" },
    { label: "init",   text: "in-degree 0 nodes → queue=[0]; results=[]" },
    { label: "pop 0",  text: "results=[0]; inDegree[1]=0 → enqueue; inDegree[2]=0 → enqueue; queue=[1,2]" },
    { label: "pop 1",  text: "results=[0,1]; inDegree[3]=1; queue=[2]" },
    { label: "pop 2",  text: "results=[0,1,2]; inDegree[3]=0 → enqueue; queue=[3]" },
    { label: "pop 3",  text: "results=[0,1,2,3]; queue=[]" },
    { label: "done",   text: "results.count=4 == numCourses=4 → return [0,1,2,3]" },
  ]}
/>

<SectionHeader n={6} title="Step-by-Step Solution" />

<StepList>
  <StepItem title="Build adjacency list and in-degree array">
    For each `[course, prereq]`: `adjList[prereq].append(course)`, `inDegree[course] += 1`.
  </StepItem>
  <StepItem title="Seed queue with zero-in-degree courses">
    All courses with `inDegree == 0` have no prerequisites and can be taken immediately.
  </StepItem>
  <StepItem title="BFS — collect topological order">
    Dequeue a course, append to `results`, decrement each dependent's in-degree; enqueue dependents whose in-degree reaches 0.
  </StepItem>
  <StepItem title="Return order or empty">
    If `results.count == numCourses`, return `results`. Otherwise return `[]`.
  </StepItem>
</StepList>

<SectionHeader n={7} title="Code Implementation" />

```swift
class Solution {
  func findOrder(_ numCourses: Int, _ prerequisites: [[Int]]) -> [Int] {
    var inDegree = Array(repeating: 0, count: numCourses)
    var results  = [Int]()
    var adjList  = [Int: [Int]]()

    for pairs in prerequisites {
      let course = pairs[0], prereq = pairs[1]
      adjList[prereq, default: []].append(course)
      inDegree[course] += 1
    }

    var queue = [Int]()
    for (index, count) in inDegree.enumerated() {
      if count == 0 { queue.append(index) }
    }

    while !queue.isEmpty {
      let node = queue.removeFirst()
      results.append(node)
      guard let neighbors = adjList[node] else { continue }
      for neighbor in neighbors {
        inDegree[neighbor] -= 1
        if inDegree[neighbor] == 0 { queue.append(neighbor) }
      }
    }

    return results.count == numCourses ? results : []
  }
}
```

<SectionHeader n={8} title="Complexity Analysis" />

<ComplexityTable
  rows={[
    { approach: "Kahn's BFS topological sort", time: "O(V + E)", space: "O(V + E)", note: "V = numCourses, E = prerequisites.count; every node and edge processed once", highlight: true },
    { approach: "DFS with 3-color marking", time: "O(V + E)", space: "O(V)", note: "Track unvisited/in-progress/done; build result in reverse via stack" },
  ]}
/>

<SectionHeader n={9} title="Common Pitfalls" />

<CommonPitfalls
  pitfalls={[
    {
      title: "Returning results without checking the count",
      body: "If a cycle exists, results will have fewer than numCourses entries. Returning it without checking produces a partial order that skips cyclic nodes.",
      fix: "Always guard: <code>return results.count == numCourses ? results : []</code>.",
    },
    {
      title: "Confusing edge direction",
      body: "prerequisite[i] = [a, b] means b unlocks a. The directed edge is b → a (b's adjacency list contains a), and a's in-degree is incremented.",
      fix: "<code>adjList[prereq].append(course)</code> and <code>inDegree[course] += 1</code>.",
    },
    {
      title: "Not handling courses with no prerequisites",
      body: "Courses not mentioned in prerequisites have in-degree 0 and must still be included in the output.",
      fix: "The in-degree array is initialised to all zeros for numCourses entries, so unconstrained courses are automatically seeded into the queue.",
    },
  ]}
/>
