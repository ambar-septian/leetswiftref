---
id: 1466-reorder-routes-to-make-all-paths-lead-to-the-city-zero
title: "Reorder Routes to Make All Paths Lead to the City Zero"
hide_table_of_contents: true
---

import { ProblemHeader, SectionHeader, ComplexityTable, PatternFlowchart, Walkthrough, StepList, StepItem, CommonPitfalls, Callout, ReorderRoutesDiagram } from '@site/src/components/DSA';

<ProblemHeader
  number={1466}
  title="Reorder Routes to Make All Paths Lead to the City Zero"
  difficulty="Medium"
  pattern="Graph — DFS on Undirected View"
  tags={["Depth-First Search", "Breadth-First Search", "Graph Theory"]}
  leetcodeUrl="https://leetcode.com/problems/reorder-routes-to-make-all-paths-lead-to-the-city-zero/description/"
/>

<SectionHeader n={1} title="Problem Overview" />

There are `n` cities numbered from `0` to `n-1` and `n-1` roads such that there is only one way to travel between two different cities (this network forms a tree). Last year, the ministry of transport decided to orient the roads in one direction because they are too narrow.

Roads are represented by `connections` where `connections[i] = [a, b]` represents a road from city `a` to city `b`.

This year, there will be a big event in the capital (city `0`), and many people want to travel to it. Your task consists of reorienting some roads such that each city can visit the city `0`. Return the **minimum** number of edges changed.

It's guaranteed that each city can reach city 0 after reordering.

**Constraints:**
- `2 <= n <= 5 * 10^4`
- `connections.length == n - 1`
- `0 <= connections[i][0], connections[i][1] <= n - 1`
- `connections[i][0] != connections[i][1]`

<SectionHeader n={2} title="Examples" />

#### Example 1

```
Input:  n=6, connections=[[0,1],[1,3],[2,3],[4,0],[4,5]]
Output: 3
Explanation: Change the direction of edges [0,1],[1,3],[4,5].
```

#### Example 2

```
Input:  n=5, connections=[[1,0],[1,2],[3,2],[3,4]]
Output: 2
```

<SectionHeader n={3} title="Pattern Recognition" />

<PatternFlowchart
  steps={[
    { question: "Is this a tree (n nodes, n-1 edges)?", verdict: "yes", reason: "BFS or DFS from any node will visit all nodes exactly once." },
    { question: "How do we detect which edges need reversal?", verdict: "yes", reason: "Build an undirected adjacency list labelling each edge as original (true) or reversed (false). DFS from city 0: every original edge (pointing away from 0) needs reversal." },
    { question: "Can we avoid storing the full edge set?", verdict: "yes", reason: "Using (node, isOriginal) tuples in the adjacency list avoids a separate HashSet lookup." },
  ]}
  answer="DFS on undirected view — count original edges traversed away from city 0"
/>

<SectionHeader n={4} title="Approach" />

Build an undirected adjacency list where each edge is stored in both directions with a flag: `isOriginal = true` means the original direction goes away from city 0 (it needs reversal when we traverse it during DFS from 0).

DFS starts at city 0. For each unvisited neighbor, if the edge is `isOriginal`, increment the result (we need to reverse it so traffic flows toward 0).

<Callout type="green" label="Why DFS from city 0 works">
We want all edges to point toward city 0. DFS from 0 naturally explores outward. Every original (outward) edge we cross is one that needs reversal. Reversed edges (which already point inward toward 0) are free.
</Callout>

<SectionHeader n={5} title="Interactive Walkthrough" />

<ReorderRoutesDiagram />

<SectionHeader n={6} title="Step-by-Step Solution" />

<StepList>
  <StepItem title="Build adjacency list">
    For each connection `[a, b]`: add `(b, isOriginal: true)` to `neighbors[a]` and `(a, isOriginal: false)` to `neighbors[b]`.
  </StepItem>
  <StepItem title="DFS from city 0">
    Mark 0 as visited. For each unvisited neighbor: if `isOriginal` is true, increment result. Mark neighbor visited and recurse.
  </StepItem>
  <StepItem title="Return result">
    The total count of original edges traversed is the minimum number of reversals.
  </StepItem>
</StepList>

<SectionHeader n={7} title="Code Implementation" />

```swift
class Solution {
  func minReorder(_ n: Int, _ connections: [[Int]]) -> Int {
    var result = 0
    var visited = Set<Int>()
    var neighbors = [Int: [(node: Int, isOriginal: Bool)]]()

    for c in connections {
      neighbors[c[0], default: []].append((c[1], true))
      neighbors[c[1], default: []].append((c[0], false))
    }

    func dfs(_ city: Int) {
      for neighbor in neighbors[city, default: []] {
        guard !visited.contains(neighbor.node) else { continue }
        if neighbor.isOriginal { result += 1 }
        visited.insert(neighbor.node)
        dfs(neighbor.node)
      }
    }

    visited.insert(0)
    dfs(0)
    return result
  }
}
```

<SectionHeader n={8} title="Complexity Analysis" />

<ComplexityTable
  rows={[
    { approach: "DFS with edge direction flags", time: "O(n)", space: "O(n)", note: "Each node/edge visited once; adjacency list is O(n) for a tree", highlight: true },
  ]}
/>

<SectionHeader n={9} title="Common Pitfalls" />

<CommonPitfalls
  pitfalls={[
    {
      title: "Not marking neighbors as visited before recursing",
      body: "In an undirected adjacency list, each edge appears in both directions. Without a visited check, DFS revisits nodes infinitely.",
      fix: "Insert neighbor.node into visited before calling dfs(neighbor.node).",
    },
    {
      title: "Confusing edge direction",
      body: "isOriginal=true means the original edge goes FROM the current city TO the neighbor — i.e., pointing away from 0. This is the edge that needs reversal.",
      fix: "When building the adjacency list, label the forward direction (a→b) as isOriginal=true and the reverse direction (b→a) as isOriginal=false.",
    },
    {
      title: "Starting DFS from a non-zero city",
      body: "The DFS must start from city 0. Starting elsewhere produces the wrong count.",
      fix: "Insert 0 into visited and call dfs(0).",
    },
  ]}
/>
