---
id: 392-is-subsequence
title: "Is Subsequence"
sidebar_label: "392 · Is Subsequence"
hide_table_of_contents: true
---

import { ProblemHeader, SectionHeader, ComplexityTable, PatternFlowchart, Walkthrough, StepList, StepItem, CommonPitfalls, Callout } from '@site/src/components/DSA';

<ProblemHeader
  number={392}
  title="Is Subsequence"
  difficulty="Easy"
  pattern="Two Pointers"
  tags={["Two Pointers", "String", "Dynamic Programming"]}
  leetcodeUrl="https://leetcode.com/problems/is-subsequence/description/"
/>

<SectionHeader n={1} title="Problem Overview" />

Given two strings `s` and `t`, return `true` if `s` is a subsequence of `t`, or `false` otherwise.

A subsequence of a string is a new string that is formed from the original string by deleting some (can be none) of the characters without disturbing the relative order of the remaining characters. For example, `"ace"` is a subsequence of `"abcde"` while `"aec"` is not.

**Constraints:**
- `0 <= s.length <= 100`
- `0 <= t.length <= 10^4`
- `s` and `t` consist only of lowercase English letters.

**Follow-up:** If there are lots of incoming `s` values (e.g., `s1, s2, ... sk` where k is large), how would you change your code? (Binary search with pre-built index lists.)

<SectionHeader n={2} title="Examples" />

#### Example 1

```
Input:  s = "abc", t = "ahbgdc"
Output: true
Explanation: a_h_b_g_d_c — a, b, c all appear in order.
```

#### Example 2

```
Input:  s = "axc", t = "ahbgdc"
Output: false
Explanation: 'x' does not appear in t after 'a'.
```

<SectionHeader n={3} title="Pattern Recognition" />

<PatternFlowchart
  steps={[
    { question: "Do we need to match characters of s in order within t?", verdict: "yes", reason: "A subsequence preserves relative order — scan t left to right and advance s pointer only on matches." },
    { question: "Can we skip characters in t freely?", verdict: "yes", reason: "t's characters that don't match can be ignored — the t pointer always advances regardless." },
    { question: "Does every character of s need a distinct match in t?", verdict: "yes", reason: "Each match consumes one t character; the s pointer only advances on a hit." },
  ]}
  answer="Two Pointers — advance s pointer only on match, always advance t pointer"
/>

<SectionHeader n={4} title="Approach" />

Convert both strings to character arrays. Use pointer `i` for `s` and pointer `j` for `t`. While `j` is within `t` and `i` has not exhausted `s`: if the characters match, advance `i`. Always advance `j`. At the end, if `i == s.count` every character of `s` was matched in order — return `true`.

<Callout type="cyan" label="Why always advance j">
We scan t completely to find all characters of s in order. Advancing j on every iteration ensures we check every potential match position in t without double-counting.
</Callout>

<SectionHeader n={5} title="Interactive Walkthrough" />

<Walkthrough
  input='s = "abc", t = "ahbgdc"'
  expected="true"
  steps={[
    { label: "init",   text: "i=0 (s), j=0 (t)" },
    { label: "j=0",   text: "s[0]='a' == t[0]='a' → match, i=1; j=1" },
    { label: "j=1",   text: "s[1]='b' != t[1]='h' → no match; j=2" },
    { label: "j=2",   text: "s[1]='b' == t[2]='b' → match, i=2; j=3" },
    { label: "j=3",   text: "s[2]='c' != t[3]='g' → no match; j=4" },
    { label: "j=4",   text: "s[2]='c' != t[4]='d' → no match; j=5" },
    { label: "j=5",   text: "s[2]='c' == t[5]='c' → match, i=3; j=6" },
    { label: "done",  text: "i=3 == s.count=3 → return true" },
  ]}
/>

<SectionHeader n={6} title="Step-by-Step Solution" />

<StepList>
  <StepItem title="Convert to character arrays">
    `let schars = s.map { $0 }`, `let tchars = t.map { $0 }`.
  </StepItem>
  <StepItem title="Initialise two pointers">
    `var i = 0` (tracks position in s), `var j = 0` (tracks position in t).
  </StepItem>
  <StepItem title="Scan t">
    While `j less than t.count` and `i less than s.count`: if `schars[i] == tchars[j]`, increment `i`. Always increment `j`.
  </StepItem>
  <StepItem title="Check completion">
    Return `i == s.count`. If `i` reached the end, every character of `s` was matched in order.
  </StepItem>
</StepList>

<SectionHeader n={7} title="Code Implementation" />

```swift
class Solution {
  func isSubsequence(_ s: String, _ t: String) -> Bool {
    let schars = s.map { $0 }
    let tchars = t.map { $0 }
    let tcount = tchars.count
    let scount = schars.count

    var i = 0
    var j = 0

    while j < tcount, i < scount {
      if schars[i] == tchars[j] {
        i += 1
      }
      j += 1
    }

    return i == scount
  }
}
```

<SectionHeader n={8} title="Complexity Analysis" />

<ComplexityTable
  rows={[
    { approach: "Two Pointers", time: "O(n)", space: "O(n)", note: "n = t.length; character array copies take O(s.length + t.length)", highlight: true },
  ]}
/>

<SectionHeader n={9} title="Common Pitfalls" />

<CommonPitfalls
  pitfalls={[
    {
      title: "Advancing j only when characters match",
      body: "If j only advances on a match, non-matching characters in t are never skipped, causing the loop to stall on mismatches.",
      fix: "Always advance j on every iteration. Only advance i when a match is found.",
    },
    {
      title: "Returning true when s is empty",
      body: "An empty string is a valid subsequence of any string. The loop exits immediately (i == 0 == scount), and i == scount is true.",
      fix: "No special-case needed — the return condition i == scount handles the empty-s case correctly.",
    },
    {
      title: "Using String.Index instead of a character array",
      body: "Swift's String.Index arithmetic is error-prone for two-pointer patterns. Directly indexing into a String with integer subscripts is not supported.",
      fix: "Convert both strings to [Character] with .map { $0 } before indexing.",
    },
  ]}
/>
