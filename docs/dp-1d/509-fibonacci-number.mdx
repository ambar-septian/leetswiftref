---
id: 509-fibonacci-number
title: "Fibonacci Number"
sidebar_label: "509 · Fibonacci Number"
hide_table_of_contents: true
---

import { ProblemHeader, SectionHeader, ComplexityTable, PatternFlowchart, Walkthrough, StepList, StepItem, CommonPitfalls, Callout } from '@site/src/components/DSA';

<ProblemHeader
  number={509}
  title="Fibonacci Number"
  difficulty="Easy"
  pattern="Dynamic Programming (Memoization)"
  tags={["Math", "Dynamic Programming", "Recursion", "Memoization"]}
  leetcodeUrl="https://leetcode.com/problems/fibonacci-number/description/"
/>

<SectionHeader n={1} title="Problem Overview" />

The **Fibonacci numbers**, commonly denoted `F(n)`, form a sequence called the Fibonacci sequence, such that each number is the sum of the two preceding ones, starting from `0` and `1`. That is:

- `F(0) = 0`
- `F(1) = 1`
- `F(n) = F(n - 1) + F(n - 2)` for `n > 1`

Given `n`, calculate `F(n)`.

**Constraints:**
- `0 <= n <= 30`

<Callout type="amber" label="Key Constraint">
`F(0) = 0` must be checked before the `n <= 2` base case. If only `n <= 2` returns `1`, then `fib(0)` incorrectly returns `1`. The order of base-case guards matters.
</Callout>

<SectionHeader n={2} title="Examples" />

#### Example 1

```
Input:  n = 2
Output: 1
Explanation: F(2) = F(1) + F(0) = 1 + 0 = 1
```

#### Example 2

```
Input:  n = 3
Output: 2
Explanation: F(3) = F(2) + F(1) = 1 + 1 = 2
```

#### Example 3

```
Input:  n = 4
Output: 3
Explanation: F(4) = F(3) + F(2) = 2 + 1 = 3
```

<SectionHeader n={3} title="Pattern Recognition" />

<PatternFlowchart
  steps={[
    { question: "Does fib(n) depend on previously computed subproblems?", verdict: "yes", reason: "F(n) = F(n-1) + F(n-2). The same subproblems repeat many times in naive recursion — memoization removes redundant work." },
    { question: "Can we solve it bottom-up without recursion?", verdict: "yes", reason: "Keep only the last two values and iterate upward. Reduces space from O(n) to O(1)." },
    { question: "Is n small enough that memoized recursion is fine?", verdict: "yes", reason: "n <= 30 — both memoized recursion and bottom-up iteration are trivially fast." },
  ]}
  answer="Memoized Recursion (Top-Down DP) — cache each F(n) after first computation"
/>

<Callout type="green" label="Rule of Thumb">
When a recursive function calls itself with the same arguments multiple times, memoize: cache results on the way back up so each subproblem is solved exactly once. Check the cache at the top of the function, before any recursive calls.
</Callout>

<SectionHeader n={4} title="Approach" />

<Callout type="cyan" label="Core Insight">
Cache `F(n)` in a dictionary so each subproblem is solved exactly once — the exponential O(2^n) call tree collapses to a linear chain of O(n) unique computations. Store the cache on the class instance and check it at the very top of the function, before making any recursive call.
</Callout>

Use a dictionary `memo` to cache results. For `n == 0` return `0`; for `n <= 2` return `1` (both base cases). For all other `n`, check the cache first; if missing, compute `fib(n-1) + fib(n-2)`, store in `memo[n]`, and return.

<Callout type="cyan" label="Why memoization over pure recursion">
Without memoization, F(5) calls F(4) and F(3); F(4) calls F(3) again. The call tree has exponential size (O(2^n)). With memoization each value is computed exactly once, giving O(n) time and O(n) space.
</Callout>

<SectionHeader n={5} title="Interactive Walkthrough" />

<Walkthrough
  input="n = 6"
  expected="8"
  steps={[
    { label: "fib(6)", text: "Not cached. Compute fib(5) + fib(4)." },
    { label: "fib(5)", text: "Not cached. Compute fib(4) + fib(3)." },
    { label: "fib(4)", text: "Not cached. Compute fib(3) + fib(2)." },
    { label: "fib(3)", text: "Not cached. Compute fib(2) + fib(1) = 1 + 1 = 2. Store memo[3]=2." },
    { label: "fib(2)", text: "Base case → return 1. Store memo[2]=1." },
    { label: "fib(4)", text: "fib(3)=2 (cached) + fib(2)=1 (cached) = 3. Store memo[4]=3." },
    { label: "fib(5)", text: "fib(4)=3 (cached) + fib(3)=2 (cached) = 5. Store memo[5]=5." },
    { label: "fib(6)", text: "fib(5)=5 (cached) + fib(4)=3 (cached) = 8. Store memo[6]=8. Return 8." },
  ]}
/>

<SectionHeader n={6} title="Step-by-Step Solution" />

<StepList>
  <StepItem title="Handle base cases">
    If `n == 0`, return `0`. If `n less than or equal to 2`, return `1`.
  </StepItem>
  <StepItem title="Check the memo cache">
    If `memo[n]` exists, return it immediately.
  </StepItem>
  <StepItem title="Recurse and cache">
    Compute `result = fib(n - 1) + fib(n - 2)`. Store `memo[n] = result`.
  </StepItem>
  <StepItem title="Return result">
    Return `result`.
  </StepItem>
</StepList>

<SectionHeader n={7} title="Code Implementation" />

```swift
class Solution {
  var memo = [Int: Int]()

  func fib(_ n: Int) -> Int {
    if n == 0 { return 0 }
    if n <= 2  { return 1 }

    if let result = memo[n] {
      return result
    }

    let result = fib(n - 1) + fib(n - 2)
    memo[n] = result
    return result
  }
}
```

<Callout type="green" label="Swift Notes">
`memo` is stored as an instance variable on `Solution` — safe in LeetCode since a fresh instance is created per test case, but would produce stale results if the instance were reused. `if let result = memo[n]` uses optional binding to check the cache. The `n == 0` base case must precede `n <= 2` because `F(0) = 0`, not 1 — guard order is load-bearing here.
</Callout>

<SectionHeader n={8} title="Complexity Analysis" />

<Callout type="cyan" label="Time Complexity — O(n)">
With memoization each value F(0)…F(n) is computed at most once. The call tree that would otherwise expand to O(2^n) nodes is pruned to exactly n unique recursive calls by the cache lookup at the top of each frame.
</Callout>

<Callout type="green" label="Space Complexity — O(n)">
The memo dictionary stores one entry per unique subproblem (0 through n), and the call stack reaches depth n before any value is cached on the way up. Both contributions are O(n), so total space is O(n).
</Callout>

<ComplexityTable
  rows={[
    { approach: "Naive Recursion", time: "O(2^n)", space: "O(n)", note: "Exponential recomputation; stack depth O(n)" },
    { approach: "Memoized Recursion (Top-Down)", time: "O(n)", space: "O(n)", note: "Each value computed once; memo dict + call stack", highlight: true },
    { approach: "Bottom-Up Iteration", time: "O(n)", space: "O(1)", note: "Optimal — only two variables needed" },
  ]}
/>

<Callout type="amber" label="Naive Recursion vs Memoized vs Bottom-Up — which to choose?">
Always choose Bottom-Up Iteration for Fibonacci: same O(n) time but O(1) space and no call-stack overhead. Use Memoized Recursion when the top-down recursive structure makes the DP recurrence easier to reason about. Avoid Naive Recursion entirely — its O(2^n) recomputation makes it impractical beyond n ≈ 30.
</Callout>

<SectionHeader n={9} title="Common Pitfalls" />

<CommonPitfalls
  pitfalls={[
    {
      title: "Forgetting the F(0) = 0 base case",
      body: "If only n less than or equal to 2 returns 1, then fib(0) incorrectly returns 1 instead of 0.",
      fix: "Check n == 0 first and return 0, then check n less than or equal to 2 and return 1.",
    },
    {
      title: "Storing memo as an instance variable on a reused Solution object",
      body: "LeetCode creates a new Solution instance per test case, so the instance-level memo is safe here. In a context where Solution is reused, stale memo entries could corrupt results.",
      fix: "For safety, initialise memo inside the function or use a local closure-captured dictionary.",
    },
    {
      title: "Not checking the cache before recursing",
      body: "If the memo check comes after the recursive call, the memoization has no effect — each call still recomputes from scratch.",
      fix: "Always check memo[n] at the top of the function, before making any recursive call.",
    },
  ]}
/>
