---
id: 1137-n-th-tribonacci-number
title: "N-th Tribonacci Number"
sidebar_label: "1137 · N-th Tribonacci Number"
hide_table_of_contents: true
---

import { ProblemHeader, SectionHeader, ComplexityTable, PatternFlowchart, Walkthrough, StepList, StepItem, CommonPitfalls, Callout } from '@site/src/components/DSA';

<ProblemHeader
  number={1137}
  title="N-th Tribonacci Number"
  difficulty="Easy"
  pattern="Dynamic Programming (1D) — Memoization"
  tags={["Math", "Dynamic Programming", "Memoization"]}
  leetcodeUrl="https://leetcode.com/problems/n-th-tribonacci-number/description/"
/>

<SectionHeader n={1} title="Problem Overview" />

The Tribonacci sequence T(n) is defined as follows:

- T(0) = 0
- T(1) = 1
- T(2) = 1
- T(n+3) = T(n) + T(n+1) + T(n+2) for n >= 0

Given `n`, return the value of T(n).

**Constraints:**
- `0 <= n <= 37`
- The answer is guaranteed to fit within a 32-bit integer, i.e., `answer <= 2^31 - 1`.

<Callout type="amber" label="Key Constraint">
  Base cases are T(0) = 0, T(1) = 1, T(2) = 1. Without explicit handling of all three, the recursion bottoms out incorrectly. n can be 0 — return 0 immediately for that case.
</Callout>

<SectionHeader n={2} title="Examples" />

#### Example 1

```
Input:  n = 4
Output: 4
Explanation: T(4) = T(1) + T(2) + T(3) = 1 + 1 + 2 = 4
```

#### Example 2

```
Input:  n = 25
Output: 1389537
```

<SectionHeader n={3} title="Pattern Recognition" />

<PatternFlowchart
  steps={[
    { question: "Does T(n) depend only on smaller subproblems?", verdict: "yes", reason: "T(n) = T(n-1) + T(n-2) + T(n-3) — classic overlapping subproblems." },
    { question: "Are subproblems recomputed in naive recursion?", verdict: "yes", reason: "Without caching, T(37) triggers exponential calls. Memoization reduces this to O(n)." },
    { question: "Can we use bottom-up DP with O(1) space?", verdict: "yes", reason: "Keep only the last 3 values — no array needed. But top-down memoization is equally valid here." },
  ]}
  answer="Dynamic Programming — Top-down memoization or bottom-up rolling window"
/>

<Callout type="green" label="Rule of Thumb">
  When a recurrence depends only on the last k values, you need only k variables — not a full array. For Tribonacci, three rolling variables replace an O(n) dp array, reducing space to O(1).
</Callout>

<SectionHeader n={4} title="Approach" />

<Callout type="cyan" label="Core Insight">
  Tribonacci is a pure DP recurrence: T(n) = T(n-1) + T(n-2) + T(n-3). Top-down memoization stores each subproblem result once, reducing exponential recursion to O(n). The bottom-up rolling-window variant eliminates the cache entirely by keeping only the last 3 values — O(1) space.
</Callout>

The recursive structure mirrors Fibonacci but sums three predecessors. A top-down approach with a dictionary cache eliminates redundant computation: once T(k) is computed, it is stored and never recomputed.

An alternative bottom-up approach maintains three variables `a, b, c` and rolls them forward, using O(1) space. Both are correct; the top-down approach maps directly to the recurrence definition.

<Callout type="green" label="Base cases">
T(0) = 0, T(1) = 1, T(2) = 1. In the Swift implementation: `n less than or equal to 0` returns 0 and `n less than 2` returns 1 (covering n=1). For n=2, the recursion computes T(-1)+T(0)+T(1) = 0+0+1 = 1, which is correct since T(-1) hits the `n less than or equal to 0` guard.
</Callout>

<SectionHeader n={5} title="Interactive Walkthrough" />

<Walkthrough
  input="n = 5"
  expected="7"
  steps={[
    { label: "T(0)", text: "Base case: n=0, return 0.", state: "cache = {0:0}" },
    { label: "T(1)", text: "Base case: n=1, return 1.", state: "cache = {0:0, 1:1}" },
    { label: "T(2)", text: "n=2: T(-1)+T(0)+T(1) = 0+0+1 = 1.", state: "cache = {0:0, 1:1, 2:1}" },
    { label: "T(3)", text: "n=3: T(0)+T(1)+T(2) = 0+1+1 = 2.", state: "cache = {0:0, 1:1, 2:1, 3:2}" },
    { label: "T(4)", text: "n=4: T(1)+T(2)+T(3) = 1+1+2 = 4. All cached.", state: "cache = {…, 4:4}" },
    { label: "T(5)", text: "n=5: T(2)+T(3)+T(4) = 1+2+4 = 7. All cached.", state: "result = 7" },
  ]}
/>

<SectionHeader n={6} title="Step-by-Step Solution" />

<StepList>
  <StepItem title="Check cache">
    If `cache[n]` exists, return it immediately — no further work needed.
  </StepItem>
  <StepItem title="Handle base cases">
    Return `0` if `n less than or equal to 0`, return `1` if `n less than 2` (covers n=1).
  </StepItem>
  <StepItem title="Recurse and cache">
    Compute `tribonacci(n-3) + tribonacci(n-2) + tribonacci(n-1)`, store in `cache[n]`, and return it.
  </StepItem>
</StepList>

<SectionHeader n={7} title="Code Implementation" />

```swift
class Solution {
  var cache = [Int: Int]()

  func tribonacci(_ n: Int) -> Int {
    if let c = cache[n] { return c }
    if n <= 0 { return 0 }
    if n < 2  { return 1 }
    let result = tribonacci(n - 3) + tribonacci(n - 2) + tribonacci(n - 1)
    cache[n] = result
    return result
  }
}
```

<Callout type="green" label="Swift Notes">
  Use `var cache = [Int: Int]()` as an instance property so it persists across the single call (the recursive calls share it). Check `if let c = cache[n] { return c }` before base-case guards to short-circuit immediately. For the rolling-window variant: `(a, b, c) = (b, c, a + b + c)` in Swift is a clean tuple swap — no temporary variable needed.
</Callout>

<SectionHeader n={8} title="Complexity Analysis" />

<Callout type="cyan" label="Time Complexity — O(n)">
  With memoization, each subproblem T(k) is computed exactly once for k in 0..n. The cache lookup is O(1). Total: O(n) calls. The bottom-up rolling window is also O(n) with n iterations.
</Callout>

<Callout type="green" label="Space Complexity — O(n) memoization / O(1) rolling">
  Memoization stores n + 1 entries in the cache dictionary — O(n). The call stack adds another O(n) in the worst case. The bottom-up rolling-window variant uses only 3 variables — O(1) space.
</Callout>

<ComplexityTable
  rows={[
    { approach: "Naive recursion (no cache)", time: "O(3ⁿ)", space: "O(n)", note: "Exponential calls — impractical for n=37" },
    { approach: "Top-down memoization", time: "O(n)", space: "O(n)", note: "Each subproblem computed once, stored in dict", highlight: true },
    { approach: "Bottom-up rolling window", time: "O(n)", space: "O(1)", note: "Track only last 3 values — optimal space" },
  ]}
/>

<SectionHeader n={9} title="Common Pitfalls" />

<CommonPitfalls
  pitfalls={[
    {
      title: "Wrong base case boundaries",
      body: "Using n==0 and n==1 checks separately while missing n==2 causes infinite recursion or wrong results for small inputs.",
      fix: "Use n less than or equal to 0 → 0 and n less than 2 → 1. For n=2 the recursion itself computes T(-1)+T(0)+T(1)=0+0+1=1 correctly.",
    },
    {
      title: "Forgetting to cache before returning",
      body: "Without storing the result in the cache, the memoization provides no benefit and performance degrades to exponential.",
      fix: "Always set cache[n] = result before returning.",
    },
    {
      title: "Using an instance-level cache across test cases",
      body: "LeetCode may reuse the same Solution instance for multiple test cases. A stale cache from a previous call may return wrong results.",
      fix: "Initialise cache inside the function or reset it at the start of each call.",
    },
  ]}
/>
