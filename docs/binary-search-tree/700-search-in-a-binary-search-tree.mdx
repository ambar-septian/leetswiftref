---
id: 700-search-in-a-binary-search-tree
title: "Search in a Binary Search Tree"
hide_table_of_contents: true
---

import { ProblemHeader, SectionHeader, ComplexityTable, PatternFlowchart, Walkthrough, StepList, StepItem, CommonPitfalls, Callout } from '@site/src/components/DSA';
import { SearchBSTDiagram } from '@site/src/components/DSA';

<ProblemHeader
  number={700}
  title="Search in a Binary Search Tree"
  difficulty="Easy"
  pattern="BST Traversal"
  tags={["Tree", "Binary Search Tree", "Binary Tree"]}
  leetcodeUrl="https://leetcode.com/problems/search-in-a-binary-search-tree/description/"
/>

<SectionHeader n={1} title="Problem Overview" />

You are given the `root` of a binary search tree (BST) and an integer `val`.

Find the node in the BST that the node's value equals `val` and return the subtree rooted with that node. If such a node does not exist, return `null`.

**Constraints:**
- The number of nodes in the tree is in the range `[1, 5000]`.
- `1 <= Node.val <= 10^7`
- `root` is a binary search tree.
- `1 <= val <= 10^7`

<SectionHeader n={2} title="Examples" />

#### Example 1

```
Input:  root = [4,2,7,1,3], val = 2
Output: [2,1,3]
Explanation: Return the subtree rooted at node 2.
```

#### Example 2

```
Input:  root = [4,2,7,1,3], val = 5
Output: []
Explanation: Node with val 5 does not exist.
```

<SectionHeader n={3} title="Pattern Recognition" />

<PatternFlowchart
  steps={[
    { question: "Is the structure a BST (left smaller, right larger)?", verdict: "yes", reason: "The BST property lets us eliminate half the tree at every step — go left if val is smaller, right if val is larger." },
    { question: "Do we need to return the full subtree rooted at the found node?", verdict: "yes", reason: "We return the matching node directly — all its children are implicitly included." },
    { question: "Can we search iteratively instead of recursively?", verdict: "yes", reason: "An iterative loop avoids call-stack overhead and is equally straightforward for BST search." },
  ]}
  answer="Iterative BST traversal — go left when val is smaller, right when larger, return node on match"
/>

<SectionHeader n={4} title="Approach" />

Start with `next = root`. Each iteration: if `curr.val == val`, return `curr`. If `curr.val > val`, go left (`next = curr.left`). If `curr.val < val`, go right (`next = curr.right`). If `next` becomes `nil`, the node was not found — return `nil`.

<SearchBSTDiagram />

<Callout type="cyan" label="BST invariant">
At every node, all values in the left subtree are strictly less than the node value, and all values in the right subtree are strictly greater. This means we never need to explore the wrong subtree — one comparison eliminates half the remaining tree.
</Callout>

<SectionHeader n={5} title="Interactive Walkthrough" />

<Walkthrough
  input="root = [4,2,7,1,3], val = 3"
  expected="node(3)"
  steps={[
    { label: "curr=4", text: "val=3 < 4 → go left." },
    { label: "curr=2", text: "val=3 > 2 → go right." },
    { label: "curr=3", text: "val=3 == 3 → return node(3)." },
  ]}
/>

<SectionHeader n={6} title="Step-by-Step Solution" />

<StepList>
  <StepItem title="Initialise pointer">
    `var next = root`.
  </StepItem>
  <StepItem title="Loop while node exists">
    `while let curr = next`.
  </StepItem>
  <StepItem title="Compare and navigate">
    If `curr.val == val`, return `curr`. If `curr.val > val`, `next = curr.left`. Else `next = curr.right`.
  </StepItem>
  <StepItem title="Not found">
    Return `next` (which is `nil` when the loop exits).
  </StepItem>
</StepList>

<SectionHeader n={7} title="Code Implementation" />

```swift
class Solution {
  func searchBST(_ root: TreeNode?, _ val: Int) -> TreeNode? {
    var next = root

    while let curr = next {
      if curr.val == val {
        return curr
      }
      if curr.val > val {
        next = curr.left
      } else {
        next = curr.right
      }
    }

    return next
  }
}
```

<SectionHeader n={8} title="Complexity Analysis" />

<ComplexityTable
  rows={[
    { approach: "Iterative BST Search", time: "O(h)", space: "O(1)", note: "h is tree height — O(log n) for balanced, O(n) for degenerate (all left or all right). No extra stack space.", highlight: true },
  ]}
/>

<SectionHeader n={9} title="Common Pitfalls" />

<CommonPitfalls
  pitfalls={[
    {
      title: "Returning nil instead of the matching node",
      body: "Forgetting to return curr when the value matches exits the loop and returns nil.",
      fix: "Check curr.val == val first inside the loop and return curr immediately.",
    },
    {
      title: "Going right when val is smaller",
      body: "Confusing the direction (left vs right) wastes O(n) time or misses the node entirely.",
      fix: "Go left when curr.val is greater than val (target is in the smaller side), right when curr.val is less than val.",
    },
    {
      title: "Using DFS recursion unnecessarily",
      body: "A recursive solution works but uses O(h) call-stack space. The iterative approach is strictly better here.",
      fix: "Use the iterative while-loop — same logic, O(1) extra space.",
    },
  ]}
/>
