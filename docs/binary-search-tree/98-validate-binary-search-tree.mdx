---
id: 98-validate-binary-search-tree
title: "Validate Binary Search Tree"
hide_table_of_contents: true
---

import { ProblemHeader, SectionHeader, ComplexityTable, PatternFlowchart, Walkthrough, StepList, StepItem, CommonPitfalls, Callout } from '@site/src/components/DSA';
import ValidateBSTDiagram from '@site/src/components/DSA/ValidateBSTDiagram';

<ProblemHeader
  number={98}
  title="Validate Binary Search Tree"
  difficulty="Medium"
  pattern="DFS"
  tags={["Tree", "Depth-First Search", "Binary Search Tree", "Binary Tree"]}
  leetcodeUrl="https://leetcode.com/problems/validate-binary-search-tree/description/"
/>

<SectionHeader n={1} title="Problem Overview" />

Given the `root` of a binary tree, determine if it is a valid **binary search tree** (BST).

A valid BST is defined as follows:
- The left subtree of a node contains only nodes with keys **strictly less than** the node's key.
- The right subtree of a node contains only nodes with keys **strictly greater than** the node's key.
- Both the left and right subtrees must also be binary search trees.

Note: comparing only with the immediate parent is not sufficient. Every node in the left subtree must be less than the **root** of that subtree, not just its direct parent.

**Constraints**
- The number of nodes in the tree is in the range `[1, 10^4]`.
- `-2^31 <= Node.val <= 2^31 - 1`

<Callout type="green" label="Core Insight">
Pass valid (min, max) bounds down the tree. At each node verify that its value is strictly within those bounds. Recurse left with the current node's value as the new upper bound; recurse right with it as the new lower bound.
</Callout>

<SectionHeader n={2} title="Examples" />

#### Example 1
```
Input:  root = [2,1,3]
Output: true
```

#### Example 2
```
Input:  root = [5,1,4,null,null,3,6]
Output: false
Explanation: Node 4 is in the right subtree of root 5, but 4 < 5.
```

<SectionHeader n={3} title="Pattern Recognition" />

<PatternFlowchart
  steps={[
    { question: "Must every node satisfy a global ordering constraint?", verdict: "yes", reason: "Not just parent-child — a node must be within the bounds established by all its ancestors." },
    { question: "Can we encode the constraint as bounds passed down?", verdict: "yes", reason: "Each recursive call narrows the valid range: left child gets (min, root.val), right child gets (root.val, max)." },
    { question: "Can we short-circuit on the first violation?", verdict: "yes", reason: "&&-chained recursion returns false immediately when any node fails its bounds check." },
  ]}
  answer="DFS with inherited (min, max) bounds — narrow range at each level"
/>

<SectionHeader n={4} title="Approach" />

Define `isValid(_ root: TreeNode?, left: Int, right: Int)`. If `root` is nil, return `true`. Check `root.val > left && root.val < right` — if either fails, return `false`. Recurse: left child with `(left, root.val)` and right child with `(root.val, right)`. Initial call: `isValid(root, left: Int.min, right: Int.max)`.

<ValidateBSTDiagram />

<Callout type="cyan" label="Why parent-only comparison fails">
For a tree [10, 5, 15, null, null, 6, 20]: node 6 is the left child of 15 and 6 less than 15 — looks valid at the local level. But 6 is also in the right subtree of root 10, so it must be greater than 10. 6 is not greater than 10, so the tree is invalid. The bounds-passing approach catches this; the parent-only approach does not.
</Callout>

<SectionHeader n={5} title="Interactive Walkthrough" />

<Walkthrough
  input="root = [5,1,4,null,null,3,6]"
  expected="false"
  steps={[
    { label: "isValid(5)", text: "bounds=(-inf,+inf). 5 in range. Left: isValid(1, -inf, 5). Right: isValid(4, 5, +inf).", state: "checking..." },
    { label: "isValid(1)", text: "bounds=(-inf,5). 1 in range. No children. Return true.", state: "left subtree ok" },
    { label: "isValid(4)", text: "bounds=(5,+inf). 4 > 5? NO. Return false immediately.", state: "false!" },
    { label: "&&", text: "Right branch returned false. isValid(5) returns false.", state: "false" },
  ]}
/>

<SectionHeader n={6} title="Step-by-Step Solution" />

<StepList>
  <StepItem title="Base case">
    If `root` is nil, return `true` — an empty subtree is always valid.
  </StepItem>
  <StepItem title="Bounds check">
    If `root.val <= left` or `root.val >= right`, return `false`.
  </StepItem>
  <StepItem title="Recurse with narrowed bounds">
    Return `isValid(root.left, left, root.val) && isValid(root.right, root.val, right)`.
  </StepItem>
  <StepItem title="Entry call">
    `isValid(root, left: Int.min, right: Int.max)`.
  </StepItem>
</StepList>

<SectionHeader n={7} title="Code Implementation" />

```swift
class Solution {
  func isValidBST(_ root: TreeNode?) -> Bool {
    func isValid(_ root: TreeNode?, left: Int, right: Int) -> Bool {
      guard let root else { return true }
      guard root.val > left, root.val < right else { return false }
      return isValid(root.left,  left: left,      right: root.val)
          && isValid(root.right, left: root.val,  right: right)
    }
    return isValid(root, left: Int.min, right: Int.max)
  }
}
```

<SectionHeader n={8} title="Complexity Analysis" />

<ComplexityTable
  rows={[
    { approach: "DFS with bounds", time: "O(n)", space: "O(h)", note: "Each node visited once; O(h) call stack (h = height, O(n) for skewed trees)", highlight: true },
  ]}
/>

<SectionHeader n={9} title="Common Pitfalls" />

<CommonPitfalls
  pitfalls={[
    {
      title: "Comparing only with the direct parent",
      body: "Checking root.val > root.left.val && root.val < root.right.val is insufficient. A node deep in the right subtree must be greater than the tree's root, not just its own parent.",
      fix: "Pass inherited bounds down the tree. Every node must satisfy the constraints imposed by all its ancestors.",
    },
    {
      title: "Using Int.min / Int.max when node values can equal those bounds",
      body: "The constraints allow node values up to 2^31 - 1 = Int32.max. If using Int32 bounds, a node with value Int32.max would incorrectly fail the strict less-than check.",
      fix: "Use Int.min and Int.max (64-bit) as the initial bounds, or use optional bounds (nil meaning unbounded).",
    },
    {
      title: "Using <= instead of < in the bounds check",
      body: "BST property requires strictly less than and strictly greater than — equal values are not allowed. Using <= would incorrectly accept trees with duplicate values.",
      fix: "Guard with root.val > left && root.val < right (strict inequalities).",
    },
  ]}
/>
