---
id: 713-subarray-product-less-than-k
title: "Subarray Product Less Than K"
hide_table_of_contents: true
---

import { ProblemHeader, SectionHeader, ComplexityTable, PatternFlowchart, Walkthrough, StepList, StepItem, CommonPitfalls, Callout } from '@site/src/components/DSA';

<ProblemHeader
  number={713}
  title="Subarray Product Less Than K"
  difficulty="Medium"
  pattern="Variable Sliding Window"
  tags={["Array", "Binary Search", "Sliding Window", "Prefix Sum"]}
  leetcodeUrl="https://leetcode.com/problems/subarray-product-less-than-k/description/"
/>

<SectionHeader n={1} title="Problem Overview" />

Given an array of integers `nums` and an integer `k`, return the number of contiguous subarrays where the product of all the elements in the subarray is strictly less than `k`.

**Constraints:**
- `1 <= nums.length <= 3 * 10^4`
- `1 <= nums[i] <= 1000`
- `0 <= k <= 10^6`

<SectionHeader n={2} title="Examples" />

#### Example 1

```
Input:  nums = [10,5,2,6], k = 100
Output: 8
Explanation: The 8 subarrays that have product less than 100 are:
  [10], [5], [2], [6], [10,5], [5,2], [2,6], [5,2,6]
  Note that [10,5,2] and [10,5,2,6] are NOT included as their products are >= 100.
```

#### Example 2

```
Input:  nums = [1,2,3], k = 0
Output: 0
```

<SectionHeader n={3} title="Pattern Recognition" />

<PatternFlowchart
  steps={[
    { question: "Does shrinking the left boundary always reduce the product?", verdict: "yes", reason: "All nums[i] >= 1, so dividing by the outgoing element never increases the product. The window is monotone in product as we shrink." },
    { question: "How many new subarrays does adding nums[right] create?", verdict: "yes", reason: "Every subarray ending at right with left endpoint from left..right is new. That is right - left + 1 new valid subarrays." },
    { question: "Do we need k less than or equal to 1 as a special case?", verdict: "yes", reason: "Since all elements are at least 1, any single element has product >= 1 >= k when k is 0 or 1 — no valid subarrays exist." },
  ]}
  answer="Variable Sliding Window — expand right, shrink left when product exceeds k, count subarrays ending at right"
/>

<SectionHeader n={4} title="Approach" />

Guard `k <= 1` and return `0`. Otherwise use a variable-size window: maintain `total` (the product of elements in `[left, right]`). For each `right`, multiply `total` by `nums[right]`. While `total >= k`, divide out `nums[left]` and advance `left`. If `total < k`, there are `right - left + 1` valid subarrays ending at `right` — add that to `result`.

<Callout type="cyan" label="Counting subarrays ending at right">
When the window [left, right] has product less than k, all subarrays ending at right with a start in [left, right] also have product less than k (since they are sub-windows). There are right - left + 1 such subarrays.
</Callout>

<SectionHeader n={5} title="Interactive Walkthrough" />

<Walkthrough
  input="nums = [10,5,2,6], k = 100"
  expected="8"
  steps={[
    { label: "r=0", text: "total=10; 10<100 → add (0-0+1)=1; result=1" },
    { label: "r=1", text: "total=10*5=50; 50<100 → add (1-0+1)=2; result=3" },
    { label: "r=2", text: "total=50*2=100; 100>=100 → shrink: total=100/10=10, left=1; 10<100 → add (2-1+1)=2; result=5" },
    { label: "r=3", text: "total=10*6=60; 60<100 → add (3-1+1)=3; result=8" },
    { label: "done", text: "return 8" },
  ]}
/>

<SectionHeader n={6} title="Step-by-Step Solution" />

<StepList>
  <StepItem title="Guard edge case">
    `if k less than or equal to 1 { return 0 }`. When k is 0 or 1, no subarray (all elements >= 1) can have a product strictly less than k.
  </StepItem>
  <StepItem title="Initialise window">
    `var left = 0`, `var total = 1`, `var result = 0`.
  </StepItem>
  <StepItem title="Expand right">
    For each `right` in `0 ..&lt; nums.count`: `total *= nums[right]`.
  </StepItem>
  <StepItem title="Shrink left while product too large">
    While `total >= k` and `left less than nums.count`: `total /= nums[left]; left += 1`.
  </StepItem>
  <StepItem title="Count valid subarrays">
    `if total < k { result += right - left + 1 }`.
  </StepItem>
  <StepItem title="Return result">
    Return `result`.
  </StepItem>
</StepList>

<SectionHeader n={7} title="Code Implementation" />

```swift
class Solution {
  func numSubarrayProductLessThanK(_ nums: [Int], _ k: Int) -> Int {
    if k <= 1 { return 0 }

    var left = 0
    var total = 1
    var result = 0

    for right in 0..<nums.count {
      total *= nums[right]

      while total >= k, left < nums.count {
        total /= nums[left]
        left += 1
      }

      if total < k {
        result += (right - left + 1)
      }
    }

    return result
  }
}
```

<SectionHeader n={8} title="Complexity Analysis" />

<ComplexityTable
  rows={[
    { approach: "Variable Sliding Window", time: "O(n)", space: "O(1)", note: "Each element is added and removed at most once; left and right each traverse the array once", highlight: true },
  ]}
/>

<SectionHeader n={9} title="Common Pitfalls" />

<CommonPitfalls
  pitfalls={[
    {
      title: "Not handling k less than or equal to 1",
      body: "When k is 0 or 1, total starts at 1 and every multiplication keeps it at least 1. The shrinking loop would drive left past right and the count formula would produce a negative value.",
      fix: "Return 0 immediately when k less than or equal to 1 — no subarray of positive integers can have a product strictly less than 1.",
    },
    {
      title: "Counting subarrays with the wrong formula",
      body: "After shrinking, there are right - left + 1 new valid subarrays ending at right — not just 1.",
      fix: "Add right - left + 1 (not 1) to result at each step to count all subarrays with right as the rightmost element.",
    },
    {
      title: "Using multiplication overflow",
      body: "With up to 30000 elements each up to 1000, the running product can grow enormous before being shrunk. In Swift, Int is 64-bit and won't overflow for these constraints, but watch for this in other languages.",
      fix: "For Swift with these constraints, Int (64-bit) is sufficient. In languages with 32-bit int, use long or check bounds carefully.",
    },
  ]}
/>
