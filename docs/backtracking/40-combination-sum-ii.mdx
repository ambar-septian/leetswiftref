---
id: 40-combination-sum-ii
title: "Combination Sum II"
hide_table_of_contents: true
---

import { ProblemHeader, SectionHeader, ComplexityTable, PatternFlowchart, Walkthrough, StepList, StepItem, CommonPitfalls, Callout } from '@site/src/components/DSA';
import CombinationSum2Diagram from '@site/src/components/DSA/CombinationSum2Diagram';

<ProblemHeader
  number={40}
  title="Combination Sum II"
  difficulty="Medium"
  pattern="Backtracking"
  tags={["Array", "Backtracking"]}
  leetcodeUrl="https://leetcode.com/problems/combination-sum-ii/description/"
/>

<SectionHeader n={1} title="Problem Overview" />

Given a collection of candidate numbers (`candidates`) and a target number (`target`), find all unique combinations in `candidates` where the candidate numbers sum to `target`.

Each number in `candidates` may only be used **once** in the combination. The solution set must not contain duplicate combinations.

**Constraints**
- `1 <= candidates.length <= 100`
- `1 <= candidates[i] <= 50`
- `1 <= target <= 30`

<Callout type="green" label="Core Insight">
Sort the candidates first. Use `dfs(i+1)` to prevent reusing the same element. Skip duplicates at the same recursion level with `if i > index && candidates[i] == candidates[i-1]` — this prevents generating identical combinations from different positions.
</Callout>

<SectionHeader n={2} title="Examples" />

#### Example 1
```
Input:  candidates = [10,1,2,7,6,1,5], target = 8
Output: [[1,1,6],[1,2,5],[1,7],[2,6]]
```

#### Example 2
```
Input:  candidates = [2,5,2,1,2], target = 5
Output: [[1,2,2],[5]]
```

<SectionHeader n={3} title="Pattern Recognition" />

<PatternFlowchart
  steps={[
    { question: "Must we enumerate all combinations summing to target?", verdict: "yes", reason: "Need every valid subset — backtracking explores all paths while pruning." },
    { question: "Can each element be reused?", verdict: "no", reason: "Each element can only be used once — advance to dfs(i+1), not dfs(i)." },
    { question: "Are there duplicate values in the input?", verdict: "yes", reason: "Sort + skip rule eliminates duplicate combinations without a visited set." },
  ]}
  answer="Backtracking — sort, dfs(i+1) for no-reuse, skip duplicate at same level"
/>

<SectionHeader n={4} title="Approach" />

Sort `candidates` so that duplicates are adjacent. Maintain a `path` array and a `sum` running total. The recursive function `dfs(_ index)` iterates from `index` to the end:

- For each `i`, if `i > index` and `candidates[i] == candidates[i-1]`, skip — this is a duplicate at the same recursion level.
- Otherwise, append, recurse with `dfs(i+1)` (advance past the current element — no reuse), then backtrack.

<CombinationSum2Diagram />

<Callout type="cyan" label="Why the skip condition uses i > index">
When `i == index` we are picking the first element at this level — always allowed. The skip only fires when `i > index`, meaning we have already processed `candidates[i-1]` at this same level and are about to pick an identical value again, which would produce a duplicate combination.
</Callout>

<SectionHeader n={5} title="Interactive Walkthrough" />

<Walkthrough
  input="candidates = [10,1,2,7,6,1,5], target = 8  →  sorted: [1,1,2,5,6,7,10]"
  expected="[[1,1,6],[1,2,5],[1,7],[2,6]]"
  steps={[
    { label: "dfs(0)", text: "Pick candidates[0]=1. path=[1], sum=1. Recurse dfs(1).", state: "path=[1], sum=1" },
    { label: "dfs(1)", text: "Pick candidates[1]=1 (i==index, no skip). path=[1,1], sum=2. Recurse dfs(2).", state: "path=[1,1], sum=2" },
    { label: "dfs(2)→6", text: "Eventually pick 6. path=[1,1,6], sum=8 == target. Append result.", state: "results=[[1,1,6]]" },
    { label: "skip dup", text: "Back at dfs(0), i=1: candidates[1]==candidates[0] and i>index(0). Skip.", state: "i=1 skipped" },
    { label: "dfs(0)→2", text: "Pick candidates[2]=2. path=[2], sum=2. Eventually find [2,6] sum=8.", state: "results include [2,6]" },
    { label: "done", text: "All branches explored. Final results: [[1,1,6],[1,2,5],[1,7],[2,6]].", state: "4 unique combinations" },
  ]}
/>

<SectionHeader n={6} title="Step-by-Step Solution" />

<StepList>
  <StepItem title="Sort the candidates">
    `var cn = candidates.sorted()` — groups duplicates together so the skip rule works.
  </StepItem>
  <StepItem title="Base cases">
    `sum == target` → append copy of `path` and return. `sum > target` → prune and return.
  </StepItem>
  <StepItem title="Iterate from index, skipping duplicates at the same level">
    For `i` from `index` to `cn.count - 1`: if `i > index && cn[i] == cn[i-1]`, skip. Otherwise append, add to sum, recurse `dfs(i+1)`.
  </StepItem>
  <StepItem title="Backtrack">
    After each recursive call: `path.removeLast()`, `sum -= cn[i]`.
  </StepItem>
</StepList>

<SectionHeader n={7} title="Code Implementation" />

```swift
class Solution {
  func combinationSum2(_ candidates: [Int], _ target: Int) -> [[Int]] {
    var results = [[Int]]()
    var path = [Int]()
    var sum = 0
    var cn = candidates.sorted()

    func dfs(_ index: Int) {
      if sum == target {
        results.append(path)
        return
      } else if sum > target {
        return
      }

      for i in index..<cn.count {
        let c = cn[i]
        if i > index, c == cn[i - 1] {
          continue               // skip duplicate at same level
        }

        path.append(c)
        sum += c
        dfs(i + 1)              // i+1: each element used at most once

        path.removeLast()
        sum -= c
      }
    }

    dfs(0)
    return results
  }
}
```

<SectionHeader n={8} title="Complexity Analysis" />

<ComplexityTable
  rows={[
    { approach: "Backtracking with sort + skip", time: "O(2^N * N)", space: "O(N)", note: "2^N subsets; O(N) copy per result; O(N log N) sort is dominated", highlight: true },
  ]}
/>

<SectionHeader n={9} title="Common Pitfalls" />

<CommonPitfalls
  pitfalls={[
    {
      title: "Using dfs(i) instead of dfs(i+1)",
      body: "Passing i instead of i+1 allows the same element to be reused, producing invalid combinations that exceed the single-use constraint.",
      fix: "Always pass i+1 to prevent reusing the element at position i.",
    },
    {
      title: "Wrong skip condition: i > startIndex vs i > index",
      body: "The condition must use the parameter name that represents the start of the current loop — not a fixed 0. Using 0 would skip the second 1 even at the very first call level.",
      fix: "Use i > index (the parameter passed into dfs), not i > 0.",
    },
    {
      title: "Forgetting to sort before the skip rule",
      body: "The duplicate-skip rule relies on identical values being adjacent. Without sorting, candidates[i] == candidates[i-1] may fire incorrectly or miss real duplicates.",
      fix: "Always sort candidates before entering the DFS.",
    },
  ]}
/>
