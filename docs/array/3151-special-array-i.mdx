---
id: 3151-special-array-i
title: "Special Array I"
hide_table_of_contents: true
---

import { ProblemHeader, SectionHeader, ComplexityTable, PatternFlowchart, Walkthrough, StepList, StepItem, CommonPitfalls, Callout } from '@site/src/components/DSA';

<ProblemHeader
  number={3151}
  title="Special Array I"
  difficulty="Easy"
  pattern="Linear Scan — Adjacent Parity Check"
  tags={["Array"]}
  leetcodeUrl="https://leetcode.com/problems/special-array-i/description/"
/>

<SectionHeader n={1} title="Problem Overview" />

An array is considered **special** if every pair of its adjacent elements contains two numbers with **different parity** (one even and one odd).

You are given an array of integers `nums`. Return `true` if `nums` is **special**, otherwise `false`.

**Constraints:**
- `1 <= nums.length <= 100`
- `1 <= nums[i] <= 100`

<SectionHeader n={2} title="Examples" />

#### Example 1

```
Input:  nums = [1]
Output: true
Explanation: A single-element array has no adjacent pairs — trivially special.
```

#### Example 2

```
Input:  nums = [2,1,4]
Output: true
Explanation:
  (2,1): even-odd — different parity ✓
  (1,4): odd-even — different parity ✓
  All adjacent pairs alternate — special.
```

#### Example 3

```
Input:  nums = [4,3,1,6]
Output: false
Explanation:
  (4,3): even-odd — different parity ✓
  (3,1): odd-odd — same parity ✗
  Not special.
```

<SectionHeader n={3} title="Pattern Recognition" />

<PatternFlowchart
  steps={[
    { question: "Do we check every adjacent pair for opposite parity?", verdict: "yes", reason: "A single pass comparing nums[i] % 2 with nums[i-1] % 2 suffices. Return false immediately on a match." },
  ]}
  answer="Single pass — return false if any adjacent pair has the same parity"
/>

<SectionHeader n={4} title="Approach" />

Iterate from index 1. For each pair `(nums[i-1], nums[i])`, check if they have the same parity: `nums[i-1] % 2 == nums[i] % 2`. If so, return `false`. If the loop completes, return `true`.

<Callout type="green" label="Parity shortcut">
XOR of the two numbers: `(nums[i-1] ^ nums[i]) % 2 == 0` means same parity. Alternatively, `(nums[i-1] + nums[i]) % 2 == 0` also detects matching parity since even+even and odd+odd are both even.
</Callout>

<SectionHeader n={5} title="Interactive Walkthrough" />

<Walkthrough
  input="nums = [4,3,1,6]"
  expected="false"
  steps={[
    { label: "Init",  text: "n=4. Start checking adjacent pairs.",                              state: "start" },
    { label: "i=1",   text: "nums[0]=4 (even), nums[1]=3 (odd). Different parity. Continue.", state: "ok so far" },
    { label: "i=2",   text: "nums[1]=3 (odd), nums[2]=1 (odd). Same parity! Return false.",   state: "return false" },
  ]}
/>

<SectionHeader n={6} title="Step-by-Step Solution" />

<StepList>
  <StepItem title="Handle edge case">
    If `nums.count == 1`, return `true` immediately.
  </StepItem>
  <StepItem title="Check adjacent pairs">
    For `i` in `1..<nums.count`: if `nums[i] % 2 == nums[i-1] % 2`, return `false`.
  </StepItem>
  <StepItem title="Return true">
    All pairs have different parity — return `true`.
  </StepItem>
</StepList>

<SectionHeader n={7} title="Code Implementation" />

```swift
class Solution {
  func isArraySpecial(_ nums: [Int]) -> Bool {
    for i in 1..<nums.count {
      if nums[i] % 2 == nums[i - 1] % 2 { return false }
    }
    return true
  }
}
```

<SectionHeader n={8} title="Complexity Analysis" />

<ComplexityTable
  rows={[
    { approach: "Single pass parity check", time: "O(n)", space: "O(1)", note: "Early exit on first violation", highlight: true },
  ]}
/>

<SectionHeader n={9} title="Common Pitfalls" />

<CommonPitfalls
  pitfalls={[
    {
      title: "Checking if both are even OR both are odd separately",
      body: "Two conditions can be unified: if (a % 2 == b % 2), they have the same parity regardless of whether both are even or both are odd.",
      fix: "Use a single condition: nums[i] % 2 == nums[i-1] % 2.",
    },
  ]}
/>
