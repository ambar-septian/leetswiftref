---
id: 872-leaf-similar-trees
title: "Leaf-Similar Trees"
sidebar_label: "872 · Leaf-Similar Trees"
hide_table_of_contents: true
---

import { ProblemHeader, SectionHeader, ComplexityTable, PatternFlowchart, Walkthrough, StepList, StepItem, CommonPitfalls, Callout } from '@site/src/components/DSA';
import { LeafSimilarTreesDiagram } from '@site/src/components/DSA';

<ProblemHeader
  number={872}
  title="Leaf-Similar Trees"
  difficulty="Easy"
  pattern="DFS Tree Traversal"
  tags={["Tree", "Depth-First Search", "Binary Tree"]}
  leetcodeUrl="https://leetcode.com/problems/leaf-similar-trees/description/"
/>

<SectionHeader n={1} title="Problem Overview" />

Consider all the leaves of a binary tree, from left to right order. The values of those leaves form a **leaf value sequence**.

Two binary trees are considered **leaf-similar** if their leaf value sequence is the same.

Return `true` if and only if the two given trees with head nodes `root1` and `root2` are leaf-similar.

**Constraints:**
- The number of nodes in each tree will be in the range `[1, 200]`.
- Both of the given trees will have values in the range `[0, 200]`.

<Callout type="amber" label="Key Constraint">
  A leaf is a node where both left and right children are nil. A node with exactly one child is not a leaf — it is an internal node with one subtree. The leaf sequence must be collected left-to-right (recurse left before right) to match order correctly.
</Callout>

<SectionHeader n={2} title="Examples" />

#### Example 1

```
Input:  root1 = [3,5,1,6,2,9,8,null,null,7,4], root2 = [3,5,1,6,7,4,2,null,null,null,null,null,null,9,8]
Output: true
```

#### Example 2

```
Input:  root1 = [1,2,3], root2 = [1,3,2]
Output: false
```

<SectionHeader n={3} title="Pattern Recognition" />

<PatternFlowchart
  steps={[
    { question: "Do we need leaf values in left-to-right order?", verdict: "yes", reason: "DFS visiting left before right naturally produces leaves in left-to-right order — just collect them into an array." },
    { question: "Is a node a leaf when both children are nil?", verdict: "yes", reason: "A leaf has no left or right child. When both are nil, append the node's value to the sequence." },
    { question: "Can we compare sequences after two separate DFS calls?", verdict: "yes", reason: "Run dfs(root1) to get sequence 1, dfs(root2) to get sequence 2, then compare for equality." },
  ]}
  answer="DFS on each tree collecting leaves left-to-right; compare the two sequences"
/>

<Callout type="green" label="Rule of Thumb">
  When a problem requires comparing a specific traversal order across two trees, run the same DFS on each tree into separate arrays and compare. Inorder left-to-right DFS naturally produces the left-to-right leaf sequence.
</Callout>

<SectionHeader n={4} title="Approach" />

<Callout type="cyan" label="Core Insight">
  Collect leaves from each tree independently using a DFS that recurses left before right. A node is a leaf when both children are nil — append its value. For internal nodes, concatenate left and right leaf sequences recursively. Then compare the two resulting arrays for equality.
</Callout>

Define `dfs(_ root: TreeNode?) -> [Int]`. If `root` is nil, return `[]`. If both children are nil, return `[root.val]`. Otherwise return `dfs(root.left) + dfs(root.right)`. Call `dfs(root1)` and `dfs(root2)` and compare the results.

<LeafSimilarTreesDiagram />

<Callout type="cyan" label="Leaf detection">
A node is a leaf exactly when root.left == nil AND root.right == nil. A node with one child is not a leaf — it is an internal node with one subtree.
</Callout>

<SectionHeader n={5} title="Interactive Walkthrough" />

<Walkthrough
  input="root1 = [3,5,1,6,2], root2 = [3,5,1,6,2]"
  expected="true"
  steps={[
    { label: "dfs(root1)", text: "DFS left-to-right. Reach leaf 6 → [6]. Reach leaf 2 → [6,2]. Reach leaf 1 → [6,2,1]." },
    { label: "dfs(root2)", text: "Same tree. Same DFS order. Sequence 2 = [6,2,1]." },
    { label: "compare", text: "[6,2,1] == [6,2,1] → return true." },
  ]}
/>

<SectionHeader n={6} title="Step-by-Step Solution" />

<StepList>
  <StepItem title="Define DFS to collect leaves">
    `func dfs(_ root: TreeNode?) -> [Int]`. Base: `guard let root else { return [] }`.
  </StepItem>
  <StepItem title="Leaf base case">
    If `root.left == nil && root.right == nil`, return `[root.val]`.
  </StepItem>
  <StepItem title="Internal node">
    Return `dfs(root.left) + dfs(root.right)`.
  </StepItem>
  <StepItem title="Compare">
    Return `dfs(root1) == dfs(root2)`.
  </StepItem>
</StepList>

<SectionHeader n={7} title="Code Implementation" />

```swift
class Solution {
  func leafSimilar(_ root1: TreeNode?, _ root2: TreeNode?) -> Bool {
    func dfs(_ root: TreeNode?) -> [Int] {
      guard let root else { return [] }
      if root.left == nil, root.right == nil {
        return [root.val]
      }
      return dfs(root.left) + dfs(root.right)
    }

    return dfs(root1) == dfs(root2)
  }
}
```

<Callout type="green" label="Swift Notes">
  `guard let root else { return [] }` is idiomatic Swift optional unwrapping for tree nodes. `dfs(root.left) + dfs(root.right)` concatenates two `[Int]` arrays — this creates a new array at each internal node, which is acceptable here given the small tree size (up to 200 nodes). For larger trees, an `inout` accumulator avoids repeated allocations.
</Callout>

<SectionHeader n={8} title="Complexity Analysis" />

<Callout type="cyan" label="Time Complexity — O(n1 + n2)">
  DFS visits every node in each tree exactly once. The array concatenation at each internal node takes time proportional to the number of leaves in its subtree — amortised over the whole tree this is O(n). Total: O(n1 + n2).
</Callout>

<Callout type="green" label="Space Complexity — O(h1 + h2 + L)">
  Call stack depth equals the tree height: O(h1 + h2). Intermediate leaf arrays at each recursive frame add up to O(L) total allocated space, where L is the total number of leaves. For balanced trees, h is O(log n).
</Callout>

<ComplexityTable
  rows={[
    { approach: "DFS with leaf collection", time: "O(n1 + n2)", space: "O(h1 + h2 + L)", note: "n1, n2 = node counts; h = tree heights (call stack); L = total leaves (result arrays).", highlight: true },
  ]}
/>

<SectionHeader n={9} title="Common Pitfalls" />

<CommonPitfalls
  pitfalls={[
    {
      title: "Treating a single-child node as a leaf",
      body: "A node with one child is not a leaf — only nodes with both children nil are leaves.",
      fix: "Check root.left == nil AND root.right == nil (both conditions) before treating a node as a leaf.",
    },
    {
      title: "Comparing trees structurally instead of by leaf sequences",
      body: "Two trees with the same shape but different internal values can still have the same leaf sequence — structural comparison is stricter than the problem requires.",
      fix: "Only collect and compare the leaf arrays, not the full tree structure.",
    },
    {
      title: "Collecting leaves in wrong order",
      body: "Visiting right before left reverses the leaf sequence, producing an incorrect comparison.",
      fix: "Always recurse left before right: dfs(root.left) + dfs(root.right).",
    },
  ]}
/>
