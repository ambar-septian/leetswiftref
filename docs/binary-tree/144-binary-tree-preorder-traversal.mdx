---
id: 144-binary-tree-preorder-traversal
title: "Binary Tree Preorder Traversal"
sidebar_label: "144 · Binary Tree Preorder Traversal"
hide_table_of_contents: true
---

import { ProblemHeader, SectionHeader, ComplexityTable, PatternFlowchart, Walkthrough, StepList, StepItem, CommonPitfalls, Callout } from '@site/src/components/DSA';
import PreorderTraversalDiagram from '@site/src/components/DSA/PreorderTraversalDiagram';

<ProblemHeader
  number={144}
  title="Binary Tree Preorder Traversal"
  difficulty="Easy"
  pattern="DFS"
  tags={["Stack", "Tree", "Depth-First Search", "Binary Tree"]}
  leetcodeUrl="https://leetcode.com/problems/binary-tree-preorder-traversal/description/"
/>

<SectionHeader n={1} title="Problem Overview" />

Given the `root` of a binary tree, return the **preorder traversal** of its node values.

Preorder traversal visits nodes in the order: **root → left subtree → right subtree**.

**Constraints:**
- The number of nodes in the tree is in the range `[0, 100]`.
- `-100 <= Node.val <= 100`
- **Follow-up:** Recursive solution is trivial. Can you do it iteratively?

<SectionHeader n={2} title="Examples" />

#### Example 1

```
Input: root = [1,null,2,3]
Output: [1,2,3]
```

#### Example 2

```
Input: root = []
Output: []
```

#### Example 3

```
Input: root = [1]
Output: [1]
```

<SectionHeader n={3} title="Pattern Recognition" />

<PatternFlowchart
  steps={[
    { question: "Is the traversal order root-first?", verdict: "yes", reason: "Preorder = root before children. Prepend root.val to the recursive result." },
    { question: "Can the solution be expressed purely functionally?", verdict: "yes", reason: "[root.val] + preorder(left) + preorder(right) directly encodes the definition." },
    { question: "Is mutation needed?", verdict: "no", reason: "Array concatenation in Swift produces new arrays — no shared mutable state." },
  ]}
  answer="Recursive DFS — prepend root, concatenate left and right subtree results"
/>

<SectionHeader n={4} title="Approach" />

<PreorderTraversalDiagram />

The recursive solution directly encodes the preorder definition: if the root is non-nil, return `[root.val]` concatenated with the preorder result of the left subtree followed by the right subtree. The base case (nil root) returns an empty array.

<Callout type="cyan" label="Concatenation vs mutation">
Swift's array concatenation with <code>+</code> creates a new array. For very deep trees this allocates O(n) intermediate arrays. An iterative approach using an explicit stack avoids this, but the recursive version is idiomatic and clear.
</Callout>

<SectionHeader n={5} title="Interactive Walkthrough" />

<Walkthrough
  expected="[1,2,3]"
  input="root = [1,null,2,3]"
  steps={[
    { label: "preorder(1)", text: "[1] + preorder(nil) + preorder(2)" },
    { label: "preorder(nil)", text: "return []" },
    { label: "preorder(2)", text: "[2] + preorder(3) + preorder(nil)" },
    { label: "preorder(3)", text: "[3] + [] + [] = [3]" },
    { label: "preorder(2)", text: "[2] + [3] + [] = [2,3]" },
    { label: "preorder(1)", text: "[1] + [] + [2,3] = [1,2,3]" },
  ]}
/>

<SectionHeader n={6} title="Step-by-Step Solution" />

<StepList>
  <StepItem title="Guard nil root">
    `guard let root else { return [] }` — returns empty array for nil.
  </StepItem>
  <StepItem title="Build preorder array">
    `[root.val] + preorderTraversal(root.left) + preorderTraversal(root.right)`.
  </StepItem>
  <StepItem title="Return result">
    The concatenated array is the preorder traversal.
  </StepItem>
</StepList>

<SectionHeader n={7} title="Code Implementation" />

```swift
class Solution {
  func preorderTraversal(_ root: TreeNode?) -> [Int] {
    guard let root else { return [] }
    return [root.val]
      + preorderTraversal(root.left)
      + preorderTraversal(root.right)
  }
}
```

<SectionHeader n={8} title="Complexity Analysis" />

<ComplexityTable
  rows={[
    { approach: "Recursive DFS", time: "O(n)", space: "O(h)", note: "Every node visited once; O(h) call stack (h = height)", highlight: true },
    { approach: "Iterative (explicit stack)", time: "O(n)", space: "O(h)", note: "Same complexity; push right then left so left is processed first" },
  ]}
/>

<SectionHeader n={9} title="Common Pitfalls" />

<CommonPitfalls
  pitfalls={[
    {
      title: "Confusing preorder with inorder",
      body: "Inorder is left → root → right; preorder is root → left → right. A common mistake is writing the left recursive call before appending root.val.",
      fix: "Put <code>[root.val]</code> first in the concatenation, before the left subtree result.",
    },
    {
      title: "Iterative approach: pushing left before right onto the stack",
      body: "A stack is LIFO. To process the left subtree first, the right child must be pushed first so the left child sits on top.",
      fix: "In the iterative version, push <code>root.right</code> first, then <code>root.left</code>, so left is popped (processed) first.",
    },
    {
      title: "Not handling the nil root case",
      body: "Accessing root.val on a nil root causes a crash.",
      fix: "Guard against nil at the start of the function and return an empty array.",
    },
  ]}
/>
