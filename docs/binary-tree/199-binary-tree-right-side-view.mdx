---
id: 199-binary-tree-right-side-view
title: "Binary Tree Right Side View"
hide_table_of_contents: true
---

import { ProblemHeader, SectionHeader, ComplexityTable, PatternFlowchart, Walkthrough, StepList, StepItem, CommonPitfalls, Callout } from '@site/src/components/DSA';
import RightSideViewDiagram from '@site/src/components/DSA/RightSideViewDiagram';

<ProblemHeader
  number={199}
  title="Binary Tree Right Side View"
  difficulty="Medium"
  pattern="BFS Level-Order"
  tags={["Tree", "Depth-First Search", "Breadth-First Search", "Binary Tree"]}
  leetcodeUrl="https://leetcode.com/problems/binary-tree-right-side-view/description/"
/>

<SectionHeader n={1} title="Problem Overview" />

Given the `root` of a binary tree, imagine yourself standing on the **right side** of it. Return the values of the nodes you can see ordered from top to bottom.

A node is visible from the right side if it is the **rightmost node at its level** — there is no other node at the same depth that would block it when viewed from the right.

**Constraints:**
- The number of nodes in the tree is in the range `[0, 100]`.
- `-100 <= Node.val <= 100`

<SectionHeader n={2} title="Examples" />

#### Example 1

```
Input: root = [1,2,3,null,5,null,4]
Output: [1,3,4]
```

#### Example 2

```
Input: root = [1,null,3]
Output: [1,3]
```

#### Example 3

```
Input: root = []
Output: []
```

<SectionHeader n={3} title="Pattern Recognition" />

<PatternFlowchart
  steps={[
    { question: "Do we need to process nodes level by level?", verdict: "yes", reason: "The right-side view is one node per level — BFS naturally groups nodes by depth." },
    { question: "Which node at each level is visible?", verdict: "yes", reason: "The last node dequeued at each level (index == count - 1) is the rightmost." },
    { question: "Could DFS also work?", verdict: "yes", reason: "DFS visiting right before left records the first node seen at each new depth — equivalent result." },
  ]}
  answer="BFS level-order — record the last node's value at each level"
/>

<SectionHeader n={4} title="Approach" />

<RightSideViewDiagram />

Use a queue initialised with the root. At each iteration, snapshot `count = queue.count` (the width of the current level). Process exactly `count` nodes: enqueue their children, and record the value of the last one (`i == count - 1`). Repeat until the queue is empty.

<Callout type="cyan" label="Why snapshot count first">
Children are added to the queue during the same loop. Snapshotting <code>count</code> before the loop prevents mixing the current level's children into the current level's processing.
</Callout>

<SectionHeader n={5} title="Interactive Walkthrough" />

<Walkthrough
  expected="[1, 3, 4]"
  input="root = [1,2,3,5,4]"
  steps={[
    { label: "init",      text: "queue=[1]; result=[]" },
    { label: "level 0",   text: "count=1; process node 1 (i=0=count-1) → record 1; enqueue 2,3; result=[1]" },
    { label: "level 1",   text: "count=2; i=0: node 2, enqueue 5; i=1: node 3 (i=count-1) → record 3; result=[1,3]" },
    { label: "level 2",   text: "count=1 (only 5 was enqueued from node 2; node 3 had no children); wait — node 2 has child 5 and 4; let's say count=2" },
    { label: "level 2",   text: "count=2; i=0: node 5; i=1: node 4 (i=count-1) → record 4; result=[1,3,4]" },
    { label: "done",      text: "queue empty → return [1,3,4]" },
  ]}
/>

<SectionHeader n={6} title="Step-by-Step Solution" />

<StepList>
  <StepItem title="Guard nil root">
    Return `[]` immediately if `root` is nil.
  </StepItem>
  <StepItem title="Initialise queue">
    `var queue: [TreeNode?] = [root]`, `var result = [Int]()`.
  </StepItem>
  <StepItem title="BFS loop">
    While `queue` is not empty: snapshot `let count = queue.count`.
  </StepItem>
  <StepItem title="Process level">
    For `i in 0..count`: dequeue `item`. If `i == count - 1`, append `item.val` to `result`. Enqueue left and right children if non-nil.
  </StepItem>
  <StepItem title="Return result">
    Return `result` after the BFS loop.
  </StepItem>
</StepList>

<SectionHeader n={7} title="Code Implementation" />

```swift
class Solution {
  func rightSideView(_ root: TreeNode?) -> [Int] {
    guard let root else { return [] }
    var queue: [TreeNode?] = [root]
    var result = [Int]()

    while !queue.isEmpty {
      let count = queue.count

      for i in 0..<count {
        guard let item = queue.removeFirst() else { continue }
        if i == count - 1 {
          result.append(item.val)
        }
        if let left  = item.left  { queue.append(left)  }
        if let right = item.right { queue.append(right) }
      }
    }

    return result
  }
}
```

<SectionHeader n={8} title="Complexity Analysis" />

<ComplexityTable
  rows={[
    { approach: "BFS level-order", time: "O(n)", space: "O(n)", note: "Every node visited once; queue holds at most one full level (O(n) in a complete tree)", highlight: true },
    { approach: "DFS (right before left)", time: "O(n)", space: "O(h)", note: "Track max depth seen; record first node at each new depth; O(h) call stack" },
  ]}
/>

<SectionHeader n={9} title="Common Pitfalls" />

<CommonPitfalls
  pitfalls={[
    {
      title: "Not snapshotting level width before the inner loop",
      body: "Without snapshotting count before the loop, <code>queue.count</code> changes as children are enqueued, so the level boundary shifts mid-loop.",
      fix: "Always capture <code>let count = queue.count</code> before the inner for loop begins.",
    },
    {
      title: "Recording the first node instead of the last",
      body: "The first node dequeued is the leftmost, not the rightmost. The right-side view needs the last node at each level.",
      fix: "Record <code>item.val</code> only when <code>i == count - 1</code>, not when <code>i == 0</code>.",
    },
    {
      title: "Enqueuing nil children",
      body: "Appending nil to the queue and not guarding against it in the inner loop causes incorrect count calculations and crashes.",
      fix: "Only enqueue non-nil children: <code>if let left = item.left { queue.append(left) }</code>.",
    },
  ]}
/>
