---
id: 141-linked-list-cycle
title: "Linked List Cycle"
sidebar_label: "141 · Linked List Cycle"
hide_table_of_contents: true
---

import { ProblemHeader, SectionHeader, ComplexityTable, PatternFlowchart, Walkthrough, StepList, StepItem, CommonPitfalls, Callout } from '@site/src/components/DSA';
import LinkedListCycleDiagram from '@site/src/components/DSA/LinkedListCycleDiagram';

<ProblemHeader
  number={141}
  title="Linked List Cycle"
  difficulty="Easy"
  pattern="Floyd's Cycle Detection"
  tags={["Hash Table", "Linked List", "Two Pointers"]}
  leetcodeUrl="https://leetcode.com/problems/linked-list-cycle/description/"
/>

<SectionHeader n={1} title="Problem Overview" />

Given `head`, the head of a linked list, determine if the linked list has a **cycle** in it.

There is a cycle in a linked list if there is some node in the list that can be reached again by continuously following the `next` pointer. Internally, `pos` is used to denote the index of the node that the tail's `next` pointer is connected to (0-indexed). Note that `pos` is not passed as a parameter.

Return `true` if there is a cycle in the linked list. Otherwise, return `false`.

**Constraints:**
- The number of nodes in the list is in the range `[0, 10^4]`.
- `-10^5 <= Node.val <= 10^5`
- `pos` is `-1` (no cycle) or a valid index in the linked list.
- **Follow-up:** Can you solve it using O(1) memory?

<Callout type="amber" label="Key Constraint">
The follow-up asks for O(1) memory — Floyd's algorithm uses only two pointers. pos is not passed as a parameter; it only describes where the tail connects back to. A list with a single node and pos = -1 must correctly return false.
</Callout>

<SectionHeader n={2} title="Examples" />

<LinkedListCycleDiagram />

#### Example 1

```
Input: head = [3,2,0,-4], pos = 1
Output: true
Explanation: Tail node (-4) connects back to node at index 1 (value=2).
```

#### Example 2

```
Input: head = [1,2], pos = 0
Output: true
```

#### Example 3

```
Input: head = [1], pos = -1
Output: false
```

<SectionHeader n={3} title="Pattern Recognition" />

<PatternFlowchart
  steps={[
    { question: "Do we need O(1) space?", verdict: "yes", reason: "A hash set approach works but uses O(n) space. Floyd's algorithm needs only two pointers." },
    { question: "Will a fast pointer eventually catch a slow pointer in a cycle?", verdict: "yes", reason: "In a cycle, fast gains one step per iteration. Since the cycle is finite, it must lap slow." },
    { question: "Will fast reach nil if there is no cycle?", verdict: "yes", reason: "Without a cycle, fast eventually hits nil — the loop terminates and we return false." },
  ]}
  answer="Floyd's cycle detection — slow moves 1 step, fast moves 2; they meet iff a cycle exists"
/>

<Callout type="green" label="Rule of Thumb">
To detect a cycle in O(1) space: use Floyd's algorithm — slow moves 1 step, fast moves 2. They meet inside the cycle if one exists; fast reaches nil if not. Start fast at head.next to avoid an immediate false positive on the first check.
</Callout>

<SectionHeader n={4} title="Approach" />

<Callout type="cyan" label="Core Insight">
Two pointers move at different speeds through the list. If a cycle exists, the faster pointer eventually laps the slower one and they meet inside the cycle. If no cycle, the faster pointer reaches nil. The speed difference of 1 step per iteration guarantees meeting within one cycle length.
</Callout>

Initialise `slow = head` and `fast = head.next`. Each iteration: advance `slow` one step and `fast` two steps. If they ever point to the same node (`===`), a cycle exists. If `fast` or `fast.next` becomes nil, the list is acyclic.

<Callout type="cyan" label="Why start fast at head.next">
Starting both at <code>head</code> would immediately satisfy <code>slow === fast</code> before any movement. Starting fast one step ahead avoids this false positive.
</Callout>

<SectionHeader n={5} title="Interactive Walkthrough" />

<Walkthrough
  expected="true"
  input="head = [3,2,0,-4], pos=1 (cycle back to index 1)"
  steps={[
    { label: "init",   text: "slow=3(0), fast=2(1)" },
    { label: "step 1", text: "slow=2(1), fast=0(2); 2 !== 0" },
    { label: "step 2", text: "slow=0(2), fast=2(1); 0 !== 2" },
    { label: "step 3", text: "slow=-4(3), fast=-4(3); === → return true" },
  ]}
/>

<SectionHeader n={6} title="Step-by-Step Solution" />

<StepList>
  <StepItem title="Initialise pointers">
    `var slow = head`, `var fast = head?.next`.
  </StepItem>
  <StepItem title="Loop while both non-nil">
    `while slow != nil, fast != nil`.
  </StepItem>
  <StepItem title="Advance pointers">
    `slow = slow?.next`, `fast = fast?.next?.next`.
  </StepItem>
  <StepItem title="Check for meeting">
    After advancing, if `slow === fast`, return `true`.
  </StepItem>
  <StepItem title="No cycle">
    Loop exits when `fast` or `fast.next` is nil — return `false`.
  </StepItem>
</StepList>

<SectionHeader n={7} title="Code Implementation" />

```swift
class Solution {
  func hasCycle(_ head: ListNode?) -> Bool {
    var slow = head
    var fast = head?.next

    while slow != nil, fast != nil {
      slow = slow?.next
      fast = fast?.next?.next

      if slow === fast {
        return true
      }
    }

    return false
  }
}
```

<Callout type="green" label="Swift Notes">
`slow === fast` uses reference equality (===) to compare node identity — two nodes with the same val at different positions must not match. `fast?.next?.next` uses optional chaining: if `fast` or `fast.next` is nil, the chain short-circuits to nil without a crash. Initialising `fast = head?.next` rather than `head` avoids an immediate false positive on the first equality check.
</Callout>

<SectionHeader n={8} title="Complexity Analysis" />

<Callout type="cyan" label="Time Complexity — O(n)">
O(n) — in the no-cycle case, fast reaches the end in n/2 iterations. In the cycle case, once fast enters the cycle it takes at most cycle_length iterations to catch slow. Total steps are bounded by O(n).
</Callout>

<Callout type="green" label="Space Complexity — O(1) / O(n)">
O(1) for Floyd's algorithm — only two pointer variables regardless of list length. O(n) for the hash set approach which stores one ObjectIdentifier per visited node.
</Callout>

<ComplexityTable
  rows={[
    { approach: "Floyd's two-pointer", time: "O(n)", space: "O(1)", note: "Fast laps slow within at most one full cycle length", highlight: true },
    { approach: "Hash set (visited nodes)", time: "O(n)", space: "O(n)", note: "Store ObjectIdentifier of each visited node" },
  ]}
/>

<Callout type="amber" label="When to prefer Floyd's vs hash set">
Use **Floyd's algorithm** when O(1) space is required — it is the standard interview solution. Use the **hash set** when you also need to identify which node the cycle starts at, or when code clarity is prioritised over space efficiency.
</Callout>

<SectionHeader n={9} title="Common Pitfalls" />

<CommonPitfalls
  pitfalls={[
    {
      title: "Using == instead of === for pointer comparison",
      body: "Two different nodes can have the same val. Using value equality (==) would produce false positives on lists with duplicate values.",
      fix: "Use === (reference/identity equality) to check if slow and fast point to the exact same node object.",
    },
    {
      title: "Starting both pointers at head",
      body: "If slow and fast both start at head, the very first iteration check compares them before any movement, which would immediately return true for any non-empty list.",
      fix: "Start fast at head.next so the initial positions differ, then check after each movement.",
    },
    {
      title: "Advancing fast before checking for nil",
      body: "Calling fast.next.next without checking that fast.next is non-nil first causes a crash on acyclic lists with even length.",
      fix: "Swift's optional chaining handles this safely: fast?.next?.next returns nil if any link in the chain is nil.",
    },
  ]}
/>
