---
id: 2487-remove-nodes-from-linked-list
title: "Remove Nodes From Linked List"
sidebar_label: "2487 · Remove Nodes From Linked List"
hide_table_of_contents: true
---

import { ProblemHeader, SectionHeader, ComplexityTable, PatternFlowchart, Walkthrough, StepList, StepItem, CommonPitfalls, Callout, RemoveNodesLinkedListDiagram } from '@site/src/components/DSA';

<ProblemHeader
  number={2487}
  title="Remove Nodes From Linked List"
  difficulty="Medium"
  pattern="Monotonic Stack — Decreasing"
  tags={["Linked List", "Stack", "Recursion", "Monotonic Stack"]}
  leetcodeUrl="https://leetcode.com/problems/remove-nodes-from-linked-list/description/"
/>

<SectionHeader n={1} title="Problem Overview" />

You are given the `head` of a linked list.

Remove every node which has a node with a **strictly greater** value anywhere to its **right** side.

Return the `head` of the modified linked list.

**Constraints:**
- The number of nodes in the list is in the range `[1, 10^5]`.
- `1 <= Node.val <= 10^5`

<SectionHeader n={2} title="Examples" />

#### Example 1

```
Input:  head = [5,2,13,3,8]
Output: [13,8]
Explanation:
  Nodes 5 and 2 are removed because 13 (to their right) is greater.
  Node 3 is removed because 8 (to its right) is greater.
  Nodes 13 and 8 have no greater value to their right — they survive.
```

#### Example 2

```
Input:  head = [1,1,1,1]
Output: [1,1,1,1]
Explanation: No node has a strictly greater value to its right.
```

<RemoveNodesLinkedListDiagram />

<SectionHeader n={3} title="Pattern Recognition" />

<PatternFlowchart
  steps={[
    { question: "Must we remove nodes dominated by a later greater value?", verdict: "yes", reason: "Any node smaller than something to its right is invalid. Walking right-to-left or using a monotonic stack lets us enforce this." },
    { question: "Is a monotonic decreasing stack the right tool?", verdict: "yes", reason: "Push nodes left-to-right. Before pushing, pop everything from the top that is smaller than the current node — those are dominated. The stack contains only survivors in order." },
  ]}
  answer="Monotonic decreasing stack — pop dominated nodes, rebuild list from stack"
/>

<SectionHeader n={4} title="Approach" />

Use a monotonic **decreasing** stack of `ListNode?`. Walk left-to-right. Before pushing the current node, pop all stack entries whose value is strictly less than the current node's value — these nodes have a greater value to their right and must be removed.

After processing all nodes, the stack holds the surviving nodes in order (largest-first from bottom to top... actually, since we never push a node smaller than the top, the stack is non-increasing from bottom to top). Rebuild the linked list by chaining nodes in stack order.

<Callout type="cyan" label="Rebuilding with a dummy head">
Iterate through the stack in order (bottom to top = left to right). Use a dummy `ListNode(0)` as the new head; chain each node and advance `curr`. Null the last node's `next`. Return `dummy.next`.
</Callout>

<SectionHeader n={5} title="Interactive Walkthrough" />

<Walkthrough
  input="head = [5,2,13,3,8]"
  expected="[13,8]"
  steps={[
    { label: "curr=5",  text: "Stack empty → push 5. Stack: [5].",                                              state: "stack=[5]" },
    { label: "curr=2",  text: "2 not > top(5) → push 2. Stack: [5,2].",                                        state: "stack=[5,2]" },
    { label: "curr=13", text: "13 > top(2) → pop 2. 13 > top(5) → pop 5. Stack empty → push 13. Stack: [13].", state: "stack=[13]" },
    { label: "curr=3",  text: "3 not > top(13) → push 3. Stack: [13,3].",                                      state: "stack=[13,3]" },
    { label: "curr=8",  text: "8 > top(3) → pop 3. 8 not > top(13) → push 8. Stack: [13,8].",                  state: "stack=[13,8]" },
    { label: "Rebuild", text: "Iterate stack [13,8]: chain nodes via dummy head.",                               state: "list=[13,8]" },
    { label: "Return",  text: "Return dummy.next = node(13). Output: [13,8].",                                   state: "return [13,8]" },
  ]}
/>

<SectionHeader n={6} title="Step-by-Step Solution" />

<StepList>
  <StepItem title="Build monotonic decreasing stack">
    Initialise `var stack = [ListNode?]()`. Walk `curr = head` while `curr != nil`. Before pushing, while `stack.last??.val` is less than `curr!.val`, pop from stack. Push `curr`. Advance `curr = curr?.next`.
  </StepItem>
  <StepItem title="Rebuild linked list">
    Create `var list = ListNode(0)` (dummy). Let `curr = list`. For each `node` in stack: `curr?.next = node; curr = curr?.next`. Null the last node's next: `curr?.next = nil`.
  </StepItem>
  <StepItem title="Return">
    Return `list.next`.
  </StepItem>
</StepList>

<SectionHeader n={7} title="Code Implementation" />

```swift
class Solution {
  func removeNodes(_ head: ListNode?) -> ListNode? {
    var stack = [ListNode?]()

    var curr = head
    while curr != nil {
      while let top = stack.last??.val, curr!.val > top {
        stack.removeLast()
      }
      stack.append(curr)
      curr = curr?.next
    }

    var list = ListNode(0)
    curr = list
    for node in stack {
      curr?.next = node
      curr = curr?.next
    }
    curr?.next = nil

    return list.next
  }
}
```

<SectionHeader n={8} title="Complexity Analysis" />

<ComplexityTable
  rows={[
    { approach: "Brute force (nested scan)", time: "O(n^2)", space: "O(1)", note: "For each node, scan right to check for greater values" },
    { approach: "Monotonic decreasing stack", time: "O(n)", space: "O(n)", note: "Each node pushed and popped at most once", highlight: true },
  ]}
/>

<SectionHeader n={9} title="Common Pitfalls" />

<CommonPitfalls
  pitfalls={[
    {
      title: "Using strictly less-than instead of less-than-or-equal-to when popping",
      body: "The problem says remove nodes with a strictly greater value to their right. Equal values should NOT be removed. The pop condition must be strictly greater: curr.val > top.",
      fix: "Pop condition: while curr!.val > stack.last??.val. Equal values stay.",
    },
    {
      title: "Not nulling the last node's next pointer",
      body: "Stack nodes still carry their original next pointers. The last node in the rebuilt list may point to a removed node, creating a corrupt list.",
      fix: "After rebuilding, set curr?.next = nil to terminate the list cleanly.",
    },
    {
      title: "Iterating stack in reverse",
      body: "The stack stores nodes in left-to-right insertion order (index 0 = leftmost survivor). Iterating forward gives the correct order.",
      fix: "Iterate stack with a standard for-in loop (not reversed) when rebuilding.",
    },
  ]}
/>
