---
id: 496-next-greater-element-i
title: "Next Greater Element I"
sidebar_label: "496 · Next Greater Element I"
hide_table_of_contents: true
---

import { ProblemHeader, SectionHeader, ComplexityTable, PatternFlowchart, Walkthrough, StepList, StepItem, CommonPitfalls, Callout } from '@site/src/components/DSA';

<ProblemHeader
  number={496}
  title="Next Greater Element I"
  difficulty="Easy"
  pattern="Monotonic Stack"
  tags={["Array", "Hash Table", "Stack", "Monotonic Stack"]}
  leetcodeUrl="https://leetcode.com/problems/next-greater-element-i/description/"
/>

<SectionHeader n={1} title="Problem Overview" />

The **next greater element** of some element `x` in an array is the **first greater element** that is to its right in the same array.

You are given two **distinct** integer arrays `nums1` and `nums2`, where `nums1` is a subset of `nums2`.

For each element in `nums1`, find the next greater element in `nums2` and return an array of the answers. If there is no next greater element, output `-1` for that position.

**Constraints:**
- `1 <= nums1.length <= nums2.length <= 1000`
- `0 <= nums1[i], nums2[i] <= 10^4`
- All integers in `nums1` and `nums2` are **unique**.
- All the integers of `nums1` also appear in `nums2`.

<SectionHeader n={2} title="Examples" />

#### Example 1

```
Input:  nums1 = [4,1,2], nums2 = [1,3,4,2]
Output: [-1,3,-1]
Explanation:
  4 → no element to its right is greater → -1
  1 → next greater in nums2 is 3 → 3
  2 → no element to its right is greater → -1
```

#### Example 2

```
Input:  nums1 = [2,4], nums2 = [1,2,3,4]
Output: [3,-1]
Explanation:
  2 → next greater is 3 → 3
  4 → no next greater → -1
```

<SectionHeader n={3} title="Pattern Recognition" />

<PatternFlowchart
  steps={[
    { question: "Do we need the next larger element to the right for a set of values?", verdict: "yes", reason: "Classic monotonic decreasing stack — pop when a larger element is found; that larger element is the answer for everything popped." },
    { question: "Are all values unique?", verdict: "yes", reason: "Unique values allow a hash map from value to index/answer without collision." },
    { question: "Can we precompute all next-greater mappings in O(n) and then answer queries in O(1)?", verdict: "yes", reason: "Process nums2 once with a monotonic stack to build a nextGreater map, then look up each nums1 element." },
  ]}
  answer="Monotonic Stack on nums2 — precompute next-greater map, look up nums1 values"
/>

<SectionHeader n={4} title="Approach" />

First, build a hash map `dict` from each value in `nums1` to its index in the result array. Initialise `result` with `-1` for every position. Iterate through `nums2` with a decreasing monotonic stack. When the current element `curr` is greater than the stack's top, pop from the stack: if the popped value is in `dict`, record `curr` as its next-greater answer. At the end, every element not popped kept its default `-1`.

<Callout type="cyan" label="Why only push nums1 values onto the stack">
We only need next-greater answers for values that appear in nums1. Pushing only those values means the stack never holds irrelevant elements, keeping both space and pop-check logic minimal.
</Callout>

<SectionHeader n={5} title="Interactive Walkthrough" />

<Walkthrough
  input="nums1 = [4,1,2], nums2 = [1,3,4,2]"
  expected="[-1, 3, -1]"
  steps={[
    { label: "init",       text: "dict={4:0, 1:1, 2:2}; result=[-1,-1,-1]; stack=[]" },
    { label: "nums2[0]=1", text: "curr=1; stack empty; 1 in dict → push 1; stack=[1]" },
    { label: "nums2[1]=3", text: "curr=3 > top(1); pop 1 → dict[1]=index 1 → result[1]=3; stack=[]; 3 not in dict → skip push" },
    { label: "nums2[2]=4", text: "curr=4; stack empty; 4 in dict → push 4; stack=[4]" },
    { label: "nums2[3]=2", text: "curr=2; 2 < top(4) → no pop; 2 in dict → push 2; stack=[4,2]" },
    { label: "done",       text: "Stack leftover [4,2] keep result=-1; return [-1,3,-1]" },
  ]}
/>

<SectionHeader n={6} title="Step-by-Step Solution" />

<StepList>
  <StepItem title="Build index map for nums1">
    Map each `nums1[i]` to index `i` in a dictionary `dict`.
  </StepItem>
  <StepItem title="Initialise result array">
    `var result = Array(repeating: -1, count: nums1.count)`.
  </StepItem>
  <StepItem title="Scan nums2 with a monotonic stack">
    For each `curr` in `nums2`: while `stack.last` exists and `curr > stack.last`, pop the top value. If it is in `dict`, set `result[dict[val]!] = curr`.
  </StepItem>
  <StepItem title="Push only nums1 values">
    After popping, if `dict[curr] != nil`, push `curr` onto the stack.
  </StepItem>
  <StepItem title="Return result">
    Elements never popped retain `-1` from initialisation.
  </StepItem>
</StepList>

<SectionHeader n={7} title="Code Implementation" />

```swift
class Solution {
  func nextGreaterElement(_ nums1: [Int], _ nums2: [Int]) -> [Int] {
    var dict = [Int: Int]()
    for i in 0..<nums1.count {
      dict[nums1[i]] = i
    }

    var stack = [Int]()
    var result = Array(repeating: -1, count: nums1.count)

    for curr in nums2 {
      while let last = stack.last, curr > last {
        let val = stack.removeLast()
        if let index = dict[val] {
          result[index] = curr
        }
      }
      if dict[curr] != nil {
        stack.append(curr)
      }
    }

    return result
  }
}
```

<SectionHeader n={8} title="Complexity Analysis" />

<ComplexityTable
  rows={[
    { approach: "Monotonic Stack", time: "O(m + n)", space: "O(m)", note: "m = nums1.length, n = nums2.length; stack and dict hold at most m elements", highlight: true },
  ]}
/>

<SectionHeader n={9} title="Common Pitfalls" />

<CommonPitfalls
  pitfalls={[
    {
      title: "Pushing all nums2 values onto the stack",
      body: "Pushing values not in nums1 wastes stack space and requires an extra dict-check on every pop. It still gives correct results but pollutes the stack.",
      fix: "Only push a value if it appears in nums1 (dict[curr] != nil). Non-nums1 values have no corresponding result slot.",
    },
    {
      title: "Returning 0 instead of -1 for elements with no next greater",
      body: "Initialising result with zeros means values without a next-greater element get 0 instead of the required -1.",
      fix: "Initialise result with Array(repeating: -1, count: nums1.count).",
    },
    {
      title: "Using value index in nums2 instead of nums1",
      body: "The result array is indexed by position in nums1, not position in nums2. Using the nums2 index produces out-of-bounds or wrong results.",
      fix: "Use dict[val] — which maps the popped value to its index in nums1 — to place the answer in the correct result slot.",
    },
  ]}
/>
